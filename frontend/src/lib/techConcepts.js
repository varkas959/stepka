// "Learn with Kai" — 5th-grade-level analogies for core programming concepts,
// organized by technology track. Each concept has a short reading (analogy +
// explainer + code) and a 1-2 question quiz, graded locally (no AI call) so
// Kai can react instantly. kaiClaim is used for the "Teach Kai" moment where
// Kai makes a claim (sometimes right, sometimes deliberately wrong) and the
// user corrects it.

const JAVA_CONCEPTS = [
  {
    id: 'classes-vs-objects',
    order: 1,
    tier: 'beginner',
    title: 'Classes vs Objects',
    tagline: 'The blueprint and the thing you build from it',
    expandReaction: "Good starting point — get this one solid and everything else clicks faster.",
    analogy: {
      emoji: '🍪',
      text: "A class is like a cookie cutter — it's the shape. An object is the actual cookie you get when you press the cutter into dough. You can make many cookies (objects) from one cutter (class), and each cookie can have its own sprinkles (data).",
    },
    explainer: "In Java, a class is a template that describes what an object will look like and what it can do — its fields (data) and methods (actions). An object is a real instance created from that class using the `new` keyword. You can create as many objects as you want from a single class, and each one holds its own separate copy of the data.",
    codeExample: {
      label: 'In real code',
      code: `class Cookie {\n  String flavor;\n}\n\nCookie choc = new Cookie();\nchoc.flavor = "chocolate chip";\n\nCookie sugar = new Cookie();\nsugar.flavor = "sugar";\n// choc and sugar are two separate objects from the same class`,
      output: [
        'choc.flavor → "chocolate chip"',
        'sugar.flavor → "sugar"',
        '✓ Two separate Cookie objects made from one class',
      ],
    },
    kaiClaim: {
      text: "Wait, so the class is the actual cookie, and the object is the cutter, right?",
      isCorrect: false,
      whyRight: "Actually it's the other way around — the class is the cutter (the shape/template), the object is the cookie you actually get.",
    },
    quiz: [
      {
        id: 'classes-vs-objects-q1',
        question: 'If Cookie is a class, what is choc in the code above?',
        options: ['An object made from the Cookie class', 'A method inside the class', 'A brand new class'],
        correctIndex: 0,
        explainCorrect: "Right — choc is a real cookie made using the Cookie cutter (class).",
        explainIncorrect: "Not quite — choc was created with `new Cookie()`, which makes an object, not a class or a method.",
        hint: "Look at the keyword right before Cookie() in that line — it's the same keyword used any time something new gets created.",
      },
    ],
    interviewChallenge: [
      "Why can multiple objects from the same class hold completely different data?",
      "What actually happens in memory when you call `new`?",
      "Is a class itself ever stored on the heap the same way an object is?",
    ],
  },
  {
    id: 'encapsulation',
    order: 2,
    tier: 'beginner',
    title: 'Encapsulation',
    tagline: 'Hiding the messy stuff behind a clean label',
    expandReaction: "Great choice — this one confuses almost everyone at first.",
    analogy: {
      emoji: '💊',
      text: "Encapsulation is like a fever capsule your mother gives you: you don't see what's inside, but taking it reduces your fever. You don't need to know the exact chemicals — you just need to know 'take one, fever goes down.' The capsule hides the complicated part and only shows you the simple result.",
    },
    explainer: "In Java, a class hides its internal data (fields) by marking them `private`, and only exposes safe, controlled ways to use that data through public methods. Other code doesn't need to know or touch the internal details — it just calls the method and gets the result, the same way you don't need to know the capsule's ingredients to feel better.",
    codeExample: {
      label: 'In real code',
      code: `public class Capsule {\n  private int feverLevel = 10; // hidden inside\n\n  public void takeMedicine() {\n    feverLevel -= 2; // logic stays inside the class\n  }\n\n  public int getFeverLevel() {\n    return feverLevel; // controlled peek, not direct access\n  }\n}`,
      output: [
        'capsule.feverLevel → 🔒 (private, no direct access)',
        'capsule.takeMedicine()',
        'capsule.getFeverLevel() → 8',
        '✓ Fever reduced without ever exposing the raw data',
      ],
    },
    kaiClaim: {
      text: "So private just means other classes can't touch the data directly, they have to go through a method?",
      isCorrect: true,
      whyRight: "Exactly right — that's the whole idea of encapsulation.",
    },
    quiz: [
      {
        id: 'encapsulation-q1',
        question: 'Why do we mark a class field as private instead of public?',
        options: ['So other classes cannot change it directly without going through a method', 'So the code runs faster', 'So Java lets us compile the file'],
        correctIndex: 0,
        explainCorrect: "Exactly — private hides the 'medicine', the public method is the only door in.",
        explainIncorrect: "Not quite — private isn't about speed or compiling, it's about controlling access.",
        hint: "Think about the capsule again — why doesn't your mother let you touch the powder inside directly?",
      },
    ],
    interviewChallenge: [
      "Why is 'private field + public getter' considered safer than a plain public field?",
      "Can encapsulation exist without using any access modifiers at all?",
      "What real bug does encapsulation prevent that a fully public class wouldn't?",
    ],
  },
  {
    id: 'inheritance',
    order: 3,
    tier: 'beginner',
    title: 'Inheritance',
    tagline: 'Children get their parents\' traits — plus a few of their own',
    expandReaction: "Classic interview topic — this one shows up constantly.",
    analogy: {
      emoji: '👨‍👩‍👧',
      text: "Inheritance is like a child inheriting eye color and height from their parent — they get those traits automatically, without learning them from scratch. But the child can also have their own hobbies the parent never had. In the same way, a new class can 'inherit' everything from an existing class, and then add its own extra abilities.",
    },
    explainer: "In Java, a class can `extend` another class to automatically get all of its fields and methods, without rewriting them. The new class (the 'child' or subclass) can also add new methods or override existing ones to behave differently — it inherits, but isn't limited to exactly the same behavior as the parent.",
    codeExample: {
      label: 'In real code',
      code: `class Animal {\n  void eat() { System.out.println("eating"); }\n}\n\nclass Dog extends Animal {\n  void bark() { System.out.println("woof!"); }\n}\n\nDog d = new Dog();\nd.eat();  // inherited from Animal\nd.bark(); // Dog's own extra ability`,
      output: [
        'd.eat()',
        'eating',
        'd.bark()',
        'woof!',
        '✓ Dog inherited eat() from Animal, and added its own bark()',
      ],
    },
    kaiClaim: {
      text: "So if Dog extends Animal, that means Animal can now use Dog's bark() method too, right?",
      isCorrect: false,
      whyRight: "Not quite — inheritance only flows one direction. The child (Dog) gets the parent's stuff, not the other way around.",
    },
    quiz: [
      {
        id: 'inheritance-q1',
        question: 'In the code above, why can a Dog object call eat() even though eat() is only written inside the Animal class?',
        options: ['Because Dog extends Animal, so it inherits Animal\'s methods', 'Because eat() is a special Java keyword', 'Because Dog and Animal are the exact same class'],
        correctIndex: 0,
        explainCorrect: "Exactly — extending Animal means Dog automatically gets everything Animal has.",
        explainIncorrect: "Not quite — `eat()` isn't a keyword, and Dog and Animal are two different classes. The `extends` keyword is what connects them.",
        hint: "Look at the word right after 'class Dog' — it connects Dog to Animal.",
      },
    ],
    interviewChallenge: [
      "Why can inheritance make a codebase harder to change later?",
      "What happens if a subclass doesn't override anything at all?",
      "Can a class inherit from more than one class in Java? Why or why not?",
    ],
  },
  {
    id: 'polymorphism',
    order: 4,
    tier: 'beginner',
    title: 'Polymorphism',
    tagline: 'One button, different results depending on what it\'s pointed at',
    expandReaction: "That's a common interview trap — most people can define it but trip up explaining why.",
    analogy: {
      emoji: '📺',
      text: "Polymorphism is like a universal remote's power button — pressing the same button turns on a TV, an AC, or a fan, but each device reacts in its own way. The action ('press power') looks the same from the outside, but what actually happens depends on which device is receiving it.",
    },
    explainer: "In Java, polymorphism lets you call the same method name on different objects, and each object responds in its own way. This usually happens through method overriding — a subclass provides its own version of a method that a parent class also defines — so code that just calls `.makeSound()` gets different behavior depending on the actual object underneath, without needing to know which exact type it is.",
    codeExample: {
      label: 'In real code',
      code: `class Animal {\n  void makeSound() { System.out.println("some sound"); }\n}\nclass Dog extends Animal {\n  void makeSound() { System.out.println("Woof!"); }\n}\nclass Cat extends Animal {\n  void makeSound() { System.out.println("Meow!"); }\n}\n\nAnimal a1 = new Dog();\nAnimal a2 = new Cat();\na1.makeSound(); // Woof!\na2.makeSound(); // Meow!`,
      output: [
        'a1.makeSound()',
        'Woof!',
        'a2.makeSound()',
        'Meow!',
        '✓ Same method call, different behavior per real object',
      ],
    },
    kaiClaim: {
      text: "So the same method name can behave completely differently depending on which actual object is calling it?",
      isCorrect: true,
      whyRight: "Yes! That's polymorphism in a nutshell.",
    },
    quiz: [
      {
        id: 'polymorphism-q1',
        question: 'Why do a1.makeSound() and a2.makeSound() print different things even though both variables are typed as Animal?',
        options: ['Because the actual object underneath (Dog or Cat) decides which version of makeSound() runs', 'Because Animal has two different makeSound() methods', 'Because Java picks randomly each time'],
        correctIndex: 0,
        explainCorrect: "Right — like the remote's power button, the same call does something different depending on the real device (object) underneath.",
        explainIncorrect: "Not quite — Animal only has one makeSound() method. It's the real object type (Dog vs Cat) that decides which overridden version actually runs.",
        hint: "Think about the remote control again — the button never changes, but what's it pointed at?",
      },
    ],
    interviewChallenge: [
      "How does the JVM decide which overridden method actually runs at runtime?",
      "What's the difference between compile-time and runtime polymorphism?",
      "Why does polymorphism reduce the need for if/else chains checking object type?",
    ],
  },
  {
    id: 'abstraction',
    order: 5,
    tier: 'beginner',
    title: 'Abstraction',
    tagline: 'Using something without needing to know how it works inside',
    expandReaction: "People often mix this one up with encapsulation — good to nail the difference early.",
    analogy: {
      emoji: '🚗',
      text: "Abstraction is like driving a car — you press the accelerator and the car moves. You don't need to understand the engine, the fuel injection, or the transmission to drive. The car exposes a simple set of controls (pedal, wheel) and hides all the complicated machinery behind them.",
    },
    explainer: "In Java, abstraction means exposing only the essential, simple actions to the outside world (often through an abstract class or interface), while hiding the complicated implementation details behind them. The person using the class only needs to know what a method does, not exactly how it does it internally.",
    codeExample: {
      label: 'In real code',
      code: `abstract class Car {\n  abstract void accelerate(); // just the pedal, no engine detail\n}\n\nclass ElectricCar extends Car {\n  void accelerate() {\n    // complicated battery/motor logic hidden here\n    System.out.println("silently speeding up");\n  }\n}`,
      output: [
        'car.accelerate()',
        'silently speeding up',
        '✓ You just called accelerate() — no engine details needed',
      ],
    },
    kaiClaim: {
      text: "So abstraction means there's no code at all behind the button, right?",
      isCorrect: false,
      whyRight: "Not true — there's plenty of code in there, it's just hidden from whoever is using it.",
    },
    quiz: [
      {
        id: 'abstraction-q1',
        question: 'What does abstraction let a driver (or a programmer using a class) avoid?',
        options: ['Needing to understand the complicated internal details to use it', 'Ever needing to write any code at all', 'Having more than one class in a program'],
        correctIndex: 0,
        explainCorrect: "Exactly — you just use the simple controls, the complexity stays hidden inside.",
        explainIncorrect: "Not quite — abstraction doesn't mean no code is written, it means the complicated code is hidden behind a simple interface.",
        hint: "Think about the car analogy again — is the goal 'no engine exists' or 'you don't need to see the engine'?",
      },
    ],
    interviewChallenge: [
      "What's actually the difference between abstraction and encapsulation?",
      "Can a class be abstract with zero abstract methods? Why would you do that?",
      "Why can't you instantiate an abstract class directly?",
    ],
  },
  {
    id: 'interfaces',
    order: 6,
    tier: 'beginner',
    title: 'Interfaces',
    tagline: 'A standard shape that anything can plug into',
    expandReaction: "This one connects nicely to abstraction — good sequencing.",
    analogy: {
      emoji: '🔌',
      text: "An interface is like a power socket standard — any device with the right plug shape can be used, whether it's a lamp, a charger, or a fan. The socket doesn't care what the device does internally, only that it fits the agreed-upon shape.",
    },
    explainer: "In Java, an interface defines a set of methods that any class implementing it must provide, without saying how those methods actually work. Different classes can implement the same interface in completely different ways, just like different devices fitting the same plug shape but doing very different jobs.",
    codeExample: {
      label: 'In real code',
      code: `interface Chargeable {\n  void charge();\n}\n\nclass Phone implements Chargeable {\n  public void charge() { System.out.println("phone charging"); }\n}\n\nclass Laptop implements Chargeable {\n  public void charge() { System.out.println("laptop charging"); }\n}`,
      output: [
        'phone.charge()',
        'phone charging',
        'laptop.charge()',
        'laptop charging',
        '✓ Both fit the same Chargeable "socket", different internals',
      ],
    },
    kaiClaim: {
      text: "So an interface just says 'you must have this method', but doesn't care how you write it inside?",
      isCorrect: true,
      whyRight: "Exactly — that's the whole point of an interface.",
    },
    quiz: [
      {
        id: 'interfaces-q1',
        question: 'Why can both Phone and Laptop implement Chargeable even though they charge very differently?',
        options: ['An interface only requires the method to exist, not how it works inside', 'Phone and Laptop are secretly the same class', 'Chargeable writes the charging logic for both of them'],
        correctIndex: 0,
        explainCorrect: "Right — like a power socket, the interface only cares that the plug (method) exists, not what's on the other end.",
        explainIncorrect: "Not quite — Phone and Laptop are separate classes, and Chargeable doesn't write any logic itself, it just requires that charge() exists.",
        hint: "Think about the power socket again — does the socket care what brand of charger you plug in?",
      },
    ],
    interviewChallenge: [
      "Can an interface have method bodies in modern Java? What changed?",
      "Why would you choose an interface over an abstract class?",
      "What happens if a class implements two interfaces with the same default method?",
    ],
  },
  {
    id: 'constructors',
    order: 7,
    tier: 'beginner',
    title: 'Constructors',
    tagline: 'Filled in the moment something is created',
    expandReaction: "Last one — finish this and you've got the full OOP foundation down.",
    analogy: {
      emoji: '📜',
      text: "A constructor is like a birth certificate — it gets filled out the exact moment a baby is born, recording the name and birth date right away. In the same way, a constructor runs the instant an object is created, setting up its starting information before you use it for anything else.",
    },
    explainer: "In Java, a constructor is a special method with the same name as the class, called automatically when you use `new`. It's used to set up an object's initial state — for example, giving it a starting name or value — so the object is never left in an incomplete or undefined state.",
    codeExample: {
      label: 'In real code',
      code: `class Person {\n  String name;\n\n  Person(String givenName) { // constructor\n    name = givenName;\n  }\n}\n\nPerson p = new Person("Amit"); // constructor runs right here\nSystem.out.println(p.name); // "Amit"`,
      output: [
        'new Person("Amit")',
        '(constructor runs automatically right here)',
        'p.name → "Amit"',
        '✓ Object was ready to use the instant it was created',
      ],
    },
    kaiClaim: {
      text: "So a constructor can be called anytime later, whenever we feel like it, right?",
      isCorrect: false,
      whyRight: "Nope — a constructor only runs once, automatically, the moment the object is created with `new`.",
    },
    quiz: [
      {
        id: 'constructors-q1',
        question: 'When does the Person constructor actually run?',
        options: ['The instant `new Person("Amit")` is called', 'Only if you call it manually later', 'Only when the program finishes running'],
        correctIndex: 0,
        explainCorrect: "Exactly — just like a birth certificate, it's filled in right at the moment of creation.",
        explainIncorrect: "Not quite — constructors run automatically with `new`, you never call them manually like a regular method.",
        hint: "Think about the birth certificate again — is it filled out years later, or the moment the baby arrives?",
      },
    ],
    interviewChallenge: [
      "What does Java do if you don't write any constructor at all?",
      "Can a constructor call another constructor in the same class? How?",
      "Why can't a constructor have a return type, not even void?",
    ],
  },
  {
    id: 'method-overloading',
    order: 8,
    tier: 'beginner',
    title: 'Method Overloading',
    tagline: 'Same word, different ways of placing the order',
    expandReaction: "This is compile-time polymorphism — a phrase interviewers like to hear you use correctly.",
    analogy: {
      emoji: '🍕',
      text: "A pizza place uses the word 'order' whether you call by phone, use the app, or walk in — same word, but the restaurant recognizes which version you're using based on what you actually hand over. Method overloading is one method name with different parameter lists, and Java picks the right version based on what you pass in.",
    },
    explainer: "In Java, you can define multiple methods with the same name in a class, as long as their parameter lists differ — in number, type, or both. This is called overloading, and it's decided at compile time based on the arguments used in the call. The return type alone is not enough to make two methods count as overloaded.",
    codeExample: {
      label: 'In real code',
      code: `class Calculator {\n  int add(int a, int b) { return a + b; }\n  double add(double a, double b) { return a + b; }\n  int add(int a, int b, int c) { return a + b + c; }\n}\n\nCalculator calc = new Calculator();\ncalc.add(2, 3);       // int version\ncalc.add(2.5, 3.5);   // double version\ncalc.add(1, 2, 3);    // three-argument version`,
      output: [
        'calc.add(2, 3) → 5 (int version)',
        'calc.add(2.5, 3.5) → 6.0 (double version)',
        'calc.add(1, 2, 3) → 6 (three-argument version)',
        '✓ Same method name, Java picks the version based on the arguments passed',
      ],
    },
    kaiClaim: {
      text: "So overloading means the methods have to have different return types to count as overloaded?",
      isCorrect: false,
      whyRight: "Not quite — overloading is decided by the parameter list (number/type), not the return type. Return type alone can't tell two overloads apart.",
    },
    quiz: [
      {
        id: 'method-overloading-q1',
        question: 'What actually decides which add() method runs when you call calc.add(2, 3)?',
        options: ['The number and type of arguments you pass in', 'The return type you assign the result to', 'Whichever add() was written first in the file'],
        correctIndex: 0,
        explainCorrect: "Right — Java matches the call to the overloaded method whose parameter list fits.",
        explainIncorrect: "Not quite — return type and write-order don't decide this, the arguments passed in do.",
        hint: "Look at what's different between the three add() signatures — is it the return type or the parentheses?",
      },
    ],
    interviewChallenge: [
      "Can two methods be overloaded if they only differ by return type?",
      "How does Java resolve which overloaded version to call when argument types are ambiguous?",
      "Is overloading resolved at compile time or runtime?",
    ],
  },
  {
    id: 'method-overriding',
    order: 9,
    tier: 'beginner',
    title: 'Method Overriding',
    tagline: 'Same title, remade by a different director',
    expandReaction: "This is the real engine behind polymorphism — worth locking down right after that last concept.",
    analogy: {
      emoji: '🎭',
      text: "A movie gets remade by a different director — same title, same basic role, but this version plays it its own way. Overriding is a subclass replacing a method it inherited with its own version, while keeping the exact same name and parameter list.",
    },
    explainer: "In Java, a subclass can override a method it inherited from its parent by redefining it with the exact same name and parameter list, marked with @Override. Which version actually runs is decided at runtime based on the real object type — this is what makes polymorphism work.",
    codeExample: {
      label: 'In real code',
      code: `class Animal {\n  void makeSound() { System.out.println("some generic sound"); }\n}\n\nclass Cat extends Animal {\n  @Override\n  void makeSound() { System.out.println("Meow!"); }\n}\n\nAnimal a = new Cat();\na.makeSound();`,
      output: [
        'a.makeSound()',
        'Meow!',
        "✓ Cat's own version replaced Animal's, even though a is typed as Animal",
      ],
    },
    kaiClaim: {
      text: "So overriding means Cat just has a brand new method that happens to look similar to Animal's?",
      isCorrect: false,
      whyRight: "No — overriding requires the exact same method name and parameter list as the parent. A different name would just be a new method, not an override.",
    },
    quiz: [
      {
        id: 'method-overriding-q1',
        question: "What has to stay exactly the same between Animal's makeSound() and Cat's makeSound() for it to count as overriding?",
        options: ['The method name and parameter list', 'The actual code inside the method body', 'The class name'],
        correctIndex: 0,
        explainCorrect: "Right — same name and parameters, different body — that's what makes it an override.",
        explainIncorrect: "Not quite — the body is exactly what's allowed to differ. What must match is the method's name and parameter list.",
        hint: "Think about the movie remake again — does the title change, or just how it's performed?",
      },
    ],
    interviewChallenge: [
      "What rules must an overriding method follow regarding access modifiers?",
      "Can you override a static method? Why or why not?",
      "What happens if you change the return type slightly when overriding?",
    ],
  },
  {
    id: 'this-keyword',
    order: 10,
    tier: 'beginner',
    title: 'The `this` Keyword',
    tagline: '"This one is mine" — pointing at your own copy',
    expandReaction: "Small keyword, huge clarity once it clicks — shows up in almost every constructor.",
    analogy: {
      emoji: '🙋',
      text: "When your teacher asks 'whose homework is this?' and you say 'this is mine,' you're pointing at your own copy, not someone else's. `this` refers to the specific object currently running the code — especially useful when a parameter shares a name with a field.",
    },
    explainer: "In Java, `this` refers to the current object instance. It's most commonly used inside a constructor or method when a parameter has the same name as a field, to make clear that `this.field` means the object's own field, not the local parameter shadowing it.",
    codeExample: {
      label: 'In real code',
      code: `class Student {\n  String name;\n\n  Student(String name) {\n    this.name = name; // this.name = field, name = parameter\n  }\n}\n\nStudent s = new Student("Priya");\nSystem.out.println(s.name);`,
      output: [
        'new Student("Priya")',
        'this.name = name → the field gets set from the parameter',
        's.name → "Priya"',
        '✓ this.name pointed at the field; name (no this) was the incoming parameter',
      ],
    },
    kaiClaim: {
      text: "So this.name refers to the field, and plain name refers to the parameter that got passed in — that's how Java tells them apart?",
      isCorrect: true,
      whyRight: "Exactly right — that's precisely why `this` is needed here.",
    },
    quiz: [
      {
        id: 'this-keyword-q1',
        question: 'In the constructor above, why do we need `this.name = name;` instead of just `name = name;`?',
        options: ['Because plain "name" refers to the parameter, so this.name is needed to reach the field', 'Because Java requires `this` in every constructor', 'Because `name = name` would cause a compile error'],
        correctIndex: 0,
        explainCorrect: "Right — without `this`, `name = name` would just reassign the parameter to itself and never touch the field.",
        explainIncorrect: "Not quite — Java doesn't require `this` in every constructor, and `name = name` compiles fine, it just doesn't do what you want here.",
        hint: "Since the parameter and the field share the same name, how does Java know which one you mean without `this`?",
      },
    ],
    interviewChallenge: [
      "Can `this` be used inside a static method? Why or why not?",
      "What does `this()` (with parentheses) do inside a constructor?",
      "Is `this` ever necessary if a parameter name doesn't clash with a field?",
    ],
  },
  {
    id: 'super-keyword',
    order: 11,
    tier: 'beginner',
    title: 'The `super` Keyword',
    tagline: 'Calling your parent for their version of the answer',
    expandReaction: "Pairs naturally with inheritance and overriding — it ties those two ideas together.",
    analogy: {
      emoji: '📞',
      text: "Calling your parent to ask 'how did you handle this when you were a kid?' — you're reaching up to the parent's version of something before adding your own twist. `super` lets a subclass call its parent class's constructor or method instead of only its own.",
    },
    explainer: "In Java, `super()` calls the parent class's constructor (must be the first line if used), and `super.methodName()` calls the parent's version of a method — even one you've already overridden. This lets a subclass build on the parent's behavior instead of only replacing it.",
    codeExample: {
      label: 'In real code',
      code: `class Animal {\n  Animal() { System.out.println("Animal created"); }\n  void makeSound() { System.out.println("some sound"); }\n}\n\nclass Dog extends Animal {\n  Dog() {\n    super(); // calls Animal's constructor first\n    System.out.println("Dog created");\n  }\n  @Override\n  void makeSound() {\n    super.makeSound(); // run Animal's version too\n    System.out.println("Woof!");\n  }\n}\n\nnew Dog().makeSound();`,
      output: [
        'new Dog()',
        'Animal created',
        'Dog created',
        '.makeSound()',
        'some sound',
        'Woof!',
        "✓ super() ran the parent constructor first; super.makeSound() ran the parent's method before Dog added its own line",
      ],
    },
    kaiClaim: {
      text: "So once Dog overrides makeSound(), there's no way to still run Animal's original version at all?",
      isCorrect: false,
      whyRight: "There is — that's exactly what super.makeSound() is for. Overriding replaces the default call, but super lets you still reach the parent's version on purpose.",
    },
    quiz: [
      {
        id: 'super-keyword-q1',
        question: "What does super.makeSound() do inside Dog's overridden makeSound()?",
        options: ["Runs Animal's original makeSound() before Dog's own extra code", "Deletes Dog's override and permanently uses Animal's version", 'Causes an infinite loop between the two methods'],
        correctIndex: 0,
        explainCorrect: "Right — super reaches up to the parent's version once, then execution continues in Dog's method.",
        explainIncorrect: "Not quite — it's not permanent and it's not a loop, it's a one-time call up to the parent's version.",
        hint: "Think about calling your parent for advice — does that erase your own opinion, or just add theirs first?",
      },
    ],
    interviewChallenge: [
      "What happens if you don't explicitly call super() in a subclass constructor?",
      "Can super() and this() both be called in the same constructor?",
      "Why must super() be the first statement in a constructor if used?",
    ],
  },
  {
    id: 'static-keyword',
    order: 12,
    tier: 'beginner',
    title: 'The `static` Keyword',
    tagline: 'One shared gate, not a personal one per student',
    expandReaction: "One of the most commonly misunderstood keywords — get the 'shared, not per-object' idea solid.",
    analogy: {
      emoji: '🏫',
      text: "A school has one shared front gate — every student uses the same one, it's not a personal gate per student. A `static` field or method belongs to the class itself, shared by every object, rather than duplicated per instance.",
    },
    explainer: "In Java, `static` fields and methods belong to the class, not to any individual object — there's exactly one copy, shared across every instance, and it can be accessed without creating an object at all, using `ClassName.member`.",
    codeExample: {
      label: 'In real code',
      code: `class Counter {\n  static int totalCreated = 0; // shared by every Counter\n\n  Counter() {\n    totalCreated++;\n  }\n}\n\nnew Counter();\nnew Counter();\nnew Counter();\nSystem.out.println(Counter.totalCreated);`,
      output: [
        'new Counter() × 3',
        'Counter.totalCreated → 3',
        '✓ One shared counter across all objects, not a separate copy per object',
      ],
    },
    kaiClaim: {
      text: "So each Counter object gets its own separate totalCreated variable, just like instance variables?",
      isCorrect: false,
      whyRight: "No — that's the opposite of what static means. Static fields are shared by the whole class, one single copy, not duplicated per object.",
    },
    quiz: [
      {
        id: 'static-keyword-q1',
        question: 'Why does Counter.totalCreated end up as 3 after creating three separate Counter objects?',
        options: ['static fields are shared across every object of the class, not duplicated per object', 'Java automatically adds up all instance variables named totalCreated', 'Only the last Counter object created keeps the value'],
        correctIndex: 0,
        explainCorrect: "Right — there's exactly one totalCreated, shared and updated by every object.",
        explainIncorrect: "Not quite — Java doesn't auto-sum instance variables, and it's not just 'the last object' — it's one single shared value the whole time.",
        hint: "Think about the shared school gate — is there a separate gate for every student, or one gate everyone uses?",
      },
    ],
    interviewChallenge: [
      "Can a static method access instance (non-static) variables directly?",
      "Why can't `this` be used inside a static method?",
      "What's the risk of overusing static fields in a large application?",
    ],
  },
  {
    id: 'instance-variables',
    order: 13,
    tier: 'beginner',
    title: 'Instance Variables',
    tagline: "Your own backpack, packed your way",
    expandReaction: "The natural counterpart to static — good to contrast them back to back.",
    analogy: {
      emoji: '🎒',
      text: "Every student has their own backpack with their own stuff inside — same 'backpack' concept, but each one is personal and separately filled. Instance variables are fields where every object gets its own private copy.",
    },
    explainer: "In Java, instance variables are fields declared in a class (without `static`) that belong to each individual object. Every object created from the class gets its own separate copy — changing one object's copy never affects any other object's copy.",
    codeExample: {
      label: 'In real code',
      code: `class Student {\n  String name; // instance variable\n\n  Student(String name) { this.name = name; }\n}\n\nStudent a = new Student("Amit");\nStudent b = new Student("Riya");\nSystem.out.println(a.name);\nSystem.out.println(b.name);`,
      output: [
        'a.name → "Amit"',
        'b.name → "Riya"',
        "✓ Each Student object has its own separate name — changing one never touches the other",
      ],
    },
    kaiClaim: {
      text: "So if I change a.name, does b.name change too since they're the same class?",
      isCorrect: false,
      whyRight: "No — instance variables are separate per object. Changing a's backpack contents never touches b's backpack.",
    },
    quiz: [
      {
        id: 'instance-variables-q1',
        question: 'If you run a.name = "Kabir"; afterward, what happens to b.name?',
        options: ['Nothing — b.name stays "Riya", untouched', 'It also changes to "Kabir"', 'It becomes null'],
        correctIndex: 0,
        explainCorrect: "Right — each object's instance variables are completely independent.",
        explainIncorrect: "Not quite — instance variables aren't shared or reset between objects, each object owns its own copy.",
        hint: "Think about backpacks again — does re-packing your own backpack change what's in your friend's?",
      },
    ],
    interviewChallenge: [
      "What's the default value of an uninitialized instance variable of type int? Of an object reference?",
      "Do instance variables get memory allocated at class-loading time or object-creation time?",
      "How is an instance variable different from a local variable inside a method?",
    ],
  },
  {
    id: 'access-modifiers',
    order: 14,
    tier: 'beginner',
    title: 'Access Modifiers',
    tagline: 'A front door, a family room, and a locked diary',
    expandReaction: "Comes up in almost every code review — worth knowing the access levels cold.",
    analogy: {
      emoji: '🚪',
      text: "A house has a front door (public — anyone can knock), a family-only living room (protected — relatives allowed), and a locked personal diary (private — just you). Access modifiers control who's allowed to see or touch a field or method from outside the class.",
    },
    explainer: "In Java, `public` means accessible from anywhere, `protected` means accessible within the same package plus subclasses, default (no keyword) means accessible only within the same package, and `private` means accessible only inside the class itself. Choosing the right one is what makes encapsulation actually work.",
    codeExample: {
      label: 'In real code',
      code: `class BankAccount {\n  public String accountHolder;   // anyone can see this\n  protected double interestRate; // subclasses & same package\n  private double balance;        // only BankAccount itself\n\n  public double getBalance() {\n    return balance; // controlled access to the private field\n  }\n}`,
      output: [
        'account.accountHolder → ✅ accessible from anywhere',
        'account.interestRate → ✅ accessible from subclasses / same package only',
        'account.balance → 🔒 not accessible directly',
        'account.getBalance() → ✅ the approved way in',
      ],
    },
    kaiClaim: {
      text: "So private makes a field invisible everywhere, even inside its own class?",
      isCorrect: false,
      whyRight: "No — private only blocks OTHER classes. The class itself can always use its own private fields freely.",
    },
    quiz: [
      {
        id: 'access-modifiers-q1',
        question: 'Which access modifier would you use for balance if you want it completely hidden from every other class, reachable only through a method?',
        options: ['private', 'public', 'protected'],
        correctIndex: 0,
        explainCorrect: "Right — private locks it to the class itself, forcing everyone else through a controlled method.",
        explainIncorrect: "Not quite — public and protected both allow some outside access; private is the one that locks it down completely.",
        hint: "Think about the locked diary again — which word matches 'just for me, nobody else'?",
      },
    ],
    interviewChallenge: [
      "What's the actual difference between protected and default (package-private) access?",
      "Can a subclass in a different package override a protected method? What about a default one?",
      "Why would a public class ever have a private constructor?",
    ],
  },
  {
    id: 'composition-vs-inheritance',
    order: 15,
    tier: 'intermediate',
    title: 'Composition vs Inheritance',
    tagline: 'IS-A vehicle, or HAS-A basket?',
    expandReaction: "A favorite system-design interview question — 'favor composition over inheritance' is a real principle, not just a saying.",
    analogy: {
      emoji: '🚲',
      text: "Inheritance is 'a bike IS-A vehicle' — it inherits vehicle-ness. Composition is 'a bike HAS-A basket' — the bike is built out of parts rather than being a specialized version of something else. Composition favors building objects from other objects instead of extending a class hierarchy.",
    },
    explainer: "Inheritance reuses code by extending a class, forming an is-a relationship. Composition reuses code by holding an instance of another class as a field and delegating to it, forming a has-a relationship. Composition is often preferred in real designs because it's more flexible — you can swap the contained object without restructuring an entire class hierarchy.",
    codeExample: {
      label: 'In real code',
      code: `// Inheritance: Car IS-A Vehicle\nclass Vehicle { void move() { System.out.println("moving"); } }\nclass Car extends Vehicle {}\n\n// Composition: Car2 HAS-A Engine\nclass Engine { void start() { System.out.println("engine started"); } }\nclass Car2 {\n  private Engine engine = new Engine(); // Car2 is built from an Engine\n  void start() { engine.start(); }\n}`,
      output: [
        'new Car().move() → "moving" (inherited)',
        'new Car2().start() → "engine started" (delegated to the Engine it contains)',
        '✓ Inheritance reuses by extending; composition reuses by containing and delegating',
      ],
    },
    kaiClaim: {
      text: "So composition is just a weaker version of inheritance — only useful if extends isn't available?",
      isCorrect: false,
      whyRight: "Not at all — composition is often the preferred choice specifically because it's more flexible. It's a legitimate design decision, not a fallback.",
    },
    quiz: [
      {
        id: 'composition-vs-inheritance-q1',
        question: 'What relationship does "Car2 HAS-A Engine" describe, compared to "Car IS-A Vehicle"?',
        options: ['Composition — Car2 contains and delegates to an Engine object', 'Inheritance — Car2 extends Engine', 'They describe the exact same relationship'],
        correctIndex: 0,
        explainCorrect: "Right — HAS-A is composition; IS-A is inheritance. Two different ways to reuse code.",
        explainIncorrect: "Not quite — Car2 doesn't extend Engine here, and the two relationships aren't the same. HAS-A means composition.",
        hint: "Look at how Car2 gets access to start() — does it extend Engine, or hold one as a field?",
      },
    ],
    interviewChallenge: [
      "Why is 'favor composition over inheritance' considered good design advice?",
      "What breaks more easily: a deep inheritance hierarchy, or a composed object graph?",
      "Can you swap a composed dependency at runtime? Can you swap a parent class at runtime?",
    ],
  },
  {
    id: 'association',
    order: 16,
    tier: 'intermediate',
    title: 'Association',
    tagline: 'A teacher and a student, each independent',
    expandReaction: "The loosest of the relationship types — a good foundation before aggregation.",
    analogy: {
      emoji: '🧑‍🏫',
      text: "A teacher and a student know about each other and interact, but neither one owns the other — the student existed before the class and will exist after. Association is just a general relationship or reference between two independent classes.",
    },
    explainer: "In Java, association means one class holds a reference to another so they can interact, without either one controlling the other's creation or lifecycle. Both objects are equally independent — this is the loosest of the object relationships.",
    codeExample: {
      label: 'In real code',
      code: `class Teacher {\n  String name;\n  Teacher(String name) { this.name = name; }\n}\n\nclass Student {\n  String name;\n  Teacher teacher; // Student is associated with a Teacher\n\n  Student(String name, Teacher teacher) {\n    this.name = name;\n    this.teacher = teacher;\n  }\n}\n\nTeacher t = new Teacher("Mr. Rao");\nStudent s = new Student("Amit", t);`,
      output: [
        't exists independently as a Teacher',
        's references t through s.teacher',
        '✓ Both objects are independent — deleting the Student never deletes the Teacher, or vice versa',
      ],
    },
    kaiClaim: {
      text: "So association means Student actually owns the Teacher object and controls when it gets destroyed?",
      isCorrect: false,
      whyRight: "No — that's closer to a stronger relationship. Plain association just means they reference each other; neither controls the other's lifecycle.",
    },
    quiz: [
      {
        id: 'association-q1',
        question: 'If the Student object above is deleted, what happens to the Teacher object?',
        options: ['Nothing — the Teacher still exists independently', 'The Teacher is deleted too', 'The program crashes'],
        correctIndex: 0,
        explainCorrect: "Right — association just means they reference each other, not that one owns the other.",
        explainIncorrect: "Not quite — Teacher isn't tied to the Student's lifecycle here, and nothing about this setup would crash the program.",
        hint: "Think about the real teacher and student again — if one student leaves the school, does the teacher disappear too?",
      },
    ],
    interviewChallenge: [
      "What's the actual difference between association and dependency?",
      "Can two classes be associated with each other in both directions?",
      "Does association require one class to know the other's full implementation?",
    ],
  },
  {
    id: 'aggregation',
    order: 17,
    tier: 'intermediate',
    title: 'Aggregation',
    tagline: 'A library has books, but the books outlive it',
    expandReaction: "Builds directly on association — the difference here is just about ownership.",
    analogy: {
      emoji: '📚',
      text: "A library has books, but if the library closes, the books still exist somewhere else — they don't get destroyed with it. Aggregation is a 'has-a' relationship where the parts can live on their own, independent of the whole.",
    },
    explainer: "Aggregation is a specific, weaker form of a has-a relationship (a special case of association): one class holds a collection of another, but those contained objects can exist and be created independently of the container. This is different from composition, where the parts are usually created and destroyed along with the whole.",
    codeExample: {
      label: 'In real code',
      code: `class Book {\n  String title;\n  Book(String title) { this.title = title; }\n}\n\nclass Library {\n  List<Book> books; // Library "has" Books, but doesn't own their existence\n  Library(List<Book> books) { this.books = books; }\n}\n\nBook b1 = new Book("1984");\nLibrary library = new Library(List.of(b1));\n// even if library is discarded, b1 still exists on its own`,
      output: [
        'b1 exists on its own before library is even created',
        'library "has" b1 in its list',
        '✓ If library is discarded, b1 still exists independently — that\'s aggregation',
      ],
    },
    kaiClaim: {
      text: "So aggregation and composition are the exact same thing, just different names for one class containing another?",
      isCorrect: false,
      whyRight: "Not quite — both are 'has-a' relationships, but the difference is ownership. In aggregation the parts can outlive the whole; in composition, the parts are usually tied to the whole's lifecycle.",
    },
    quiz: [
      {
        id: 'aggregation-q1',
        question: "What's the key difference between aggregation and composition?",
        options: ['In aggregation, the contained objects can exist independently of the container', 'Aggregation only works with String fields', 'There is no real difference between them'],
        correctIndex: 0,
        explainCorrect: "Right — independence of the parts is exactly what separates aggregation from the tighter ownership of composition.",
        explainIncorrect: "Not quite — the field type doesn't matter here, and there is a meaningful difference: how tied the parts' lifecycle is to the whole.",
        hint: "Think about the library again — if the library shuts down, do the books vanish, or just move somewhere else?",
      },
    ],
    interviewChallenge: [
      "How would you represent aggregation in a UML diagram, and how is that different from composition's symbol?",
      "If the 'whole' object is deleted, why does the 'part' still exist in aggregation?",
      "Give a real example of aggregation you might see in a codebase.",
    ],
  },
  {
    id: 'dependency-injection',
    order: 18,
    tier: 'intermediate',
    title: 'Dependency Injection (basic)',
    tagline: "Handed the batteries, not built-in ones",
    expandReaction: "Last one — this is the idea behind most of Spring, so it's worth understanding from first principles.",
    analogy: {
      emoji: '🔋',
      text: "A remote control doesn't build its own batteries — you hand it batteries from outside, and it just uses whatever you give it. Dependency injection means a class doesn't create the things it depends on itself; something else hands those dependencies in, so you can swap them out easily.",
    },
    explainer: "In Java (and frameworks like Spring), dependency injection means a class receives the objects it needs — through a constructor, method, or a framework — rather than creating them internally with `new`. This makes it easy to swap in a different implementation, like a fake one for testing, without ever changing the class itself.",
    codeExample: {
      label: 'In real code',
      code: `// Without DI: tightly coupled, creates its own dependency\nclass EmailService {\n  private SmtpMailer mailer = new SmtpMailer(); // hardcoded\n}\n\n// With DI: dependency is handed in from outside\nclass EmailService2 {\n  private Mailer mailer;\n  EmailService2(Mailer mailer) { // injected via constructor\n    this.mailer = mailer;\n  }\n}\n\nnew EmailService2(new SmtpMailer());   // real mailer in production\nnew EmailService2(new FakeMailer());   // fake mailer in a test`,
      output: [
        'EmailService2 never creates its own Mailer',
        'new EmailService2(new SmtpMailer()) → uses the real one',
        'new EmailService2(new FakeMailer()) → uses a fake one for testing',
        '✓ Same class, swappable dependency, no changes needed inside EmailService2',
      ],
    },
    kaiClaim: {
      text: "So dependency injection means the class still creates its own dependency, just later instead of immediately?",
      isCorrect: false,
      whyRight: "No — the whole point is the class never creates it at all. Something outside (the caller, or a framework) creates it and hands it in.",
    },
    quiz: [
      {
        id: 'dependency-injection-q1',
        question: 'Why can EmailService2 be tested with a FakeMailer, but EmailService (without DI) cannot?',
        options: ['EmailService2 accepts its Mailer from outside, so any implementation can be passed in', 'FakeMailer is a special Java keyword', 'EmailService2 has no real functionality, only EmailService does'],
        correctIndex: 0,
        explainCorrect: "Right — because the dependency is injected rather than hardcoded, swapping in a fake for testing is trivial.",
        explainIncorrect: "Not quite — FakeMailer isn't a keyword, and EmailService2 has the same real functionality — the difference is where the Mailer comes from.",
        hint: "Think about the remote control again — can you put fake test batteries into a remote that already welded its own battery in?",
      },
    ],
    interviewChallenge: [
      "What's the difference between constructor injection and field injection, and why do most teams prefer constructor injection?",
      "How does dependency injection make unit testing easier?",
      "What problem does a DI framework like Spring actually solve compared to manual injection?",
    ],
  },
  {
    id: 'list-vs-set-vs-map',
    order: 19,
    tier: 'intermediate',
    title: 'List vs Set vs Map',
    tagline: 'A numbered shelf, a guest list, and a coat-check counter',
    expandReaction: "This decision — which container to reach for — comes up before you even write a line of logic.",
    analogy: {
      emoji: '📦',
      text: "A List is a numbered shelf — ordered, and the same item can sit in two spots. A Set is a guest list at a private party — no duplicate names allowed, and nobody really cares what order they're written in. A Map is a coat-check counter — you hand over a ticket (key) and get back exactly one coat (value).",
    },
    explainer: "List, Set, and Map are the three core collection shapes in Java. A List keeps elements in order and allows duplicates, accessed by index. A Set enforces uniqueness — adding a duplicate is silently ignored. A Map stores key-value pairs, where each key maps to exactly one value, and you look things up by key instead of position.",
    codeExample: {
      label: 'In real code',
      code: `List<String> list = new ArrayList<>();\nlist.add("Amit"); list.add("Amit"); // duplicates allowed\nSystem.out.println(list); // [Amit, Amit]\n\nSet<String> set = new HashSet<>();\nset.add("Amit"); set.add("Amit"); // duplicate ignored\nSystem.out.println(set); // [Amit]\n\nMap<String, Integer> map = new HashMap<>();\nmap.put("Amit", 23);\nSystem.out.println(map.get("Amit")); // 23`,
      output: [
        'list → [Amit, Amit] (duplicates kept)',
        'set → [Amit] (duplicate silently ignored)',
        'map.get("Amit") → 23',
        '✓ Three different containers for three different needs',
      ],
    },
    kaiClaim: {
      text: "So a Set is just a List that happens to look different, but still allows duplicates?",
      isCorrect: false,
      whyRight: "No — the defining feature of a Set is that duplicates are automatically rejected. That's exactly what separates it from a List.",
    },
    quiz: [
      {
        id: 'list-vs-set-vs-map-q1',
        question: 'If you add the same string twice to a HashSet, what happens?',
        options: ['The second add is silently ignored — the set stays at one copy', 'It gets added twice, just like a list', 'It throws an exception'],
        correctIndex: 0,
        explainCorrect: "Right — Sets automatically enforce uniqueness.",
        explainIncorrect: "Not quite — Sets don't throw on a duplicate add, they simply don't add it again, and they're not like a List which would keep both.",
        hint: "Think about the guest list again — if the same name is written twice, does the party have two of that person?",
      },
    ],
    interviewChallenge: [
      "If you need to preserve insertion order AND allow duplicates, which one do you pick?",
      "Why might a Map be a better choice than a List for looking up data by ID?",
      "Can an object be used as a Map key? What would you need to override for it to work correctly?",
    ],
  },
  {
    id: 'arraylist-vs-linkedlist',
    order: 20,
    tier: 'intermediate',
    title: 'ArrayList vs LinkedList',
    tagline: 'Numbered parking spots vs a treasure hunt chain',
    expandReaction: "A near-guaranteed interview question — always tie the answer back to Big-O, not just vibes.",
    analogy: {
      emoji: '🅿️',
      text: "ArrayList is like a row of numbered parking spots — jumping straight to spot #47 is instant, but squeezing a new car into the middle means shifting every car after it. LinkedList is like a treasure hunt chain — each clue points to the next, so inserting a new clue in the middle is easy, but finding clue #47 means walking through 46 clues first.",
    },
    explainer: "ArrayList is backed by a resizable array — O(1) random access by index, but O(n) to insert or remove in the middle since elements have to shift. LinkedList is backed by a doubly-linked chain of nodes — O(1) to insert or remove once you're at the right spot, but O(n) to reach an arbitrary index since there's no direct jump.",
    codeExample: {
      label: 'In real code',
      code: `List<Integer> arr = new ArrayList<>(List.of(1,2,3,4,5));\narr.get(3); // instant — jumps straight to index 3\n\nList<Integer> linked = new LinkedList<>(List.of(1,2,3,4,5));\nlinked.get(3); // walks through nodes 0,1,2 to reach index 3`,
      output: [
        'arr.get(3) → O(1), instant jump',
        'linked.get(3) → O(n), walks the chain node by node',
        '✓ Same result, very different cost depending on the operation',
      ],
    },
    kaiClaim: {
      text: "So LinkedList is always the better choice since it doesn't need to shift elements?",
      isCorrect: false,
      whyRight: "Not always — LinkedList is better for frequent middle inserts/removes, but ArrayList wins for random access by index, which is far more common in practice.",
    },
    quiz: [
      {
        id: 'arraylist-vs-linkedlist-q1',
        question: 'Why is arr.get(3) instant on an ArrayList but slower on a LinkedList?',
        options: ['ArrayList uses an array with direct index math; LinkedList has to walk node by node', 'LinkedList is always faster for every operation', 'ArrayList secretly converts to a LinkedList internally'],
        correctIndex: 0,
        explainCorrect: "Right — array indexing is direct math, a linked chain requires walking node to node.",
        explainIncorrect: "Not quite — LinkedList isn't universally faster, and ArrayList doesn't convert to anything, it's just backed by a real array.",
        hint: "Think about the parking spots vs the treasure hunt chain — which one lets you jump straight to spot #47?",
      },
    ],
    interviewChallenge: [
      "What's the time complexity of inserting at the front of an ArrayList vs a LinkedList?",
      "Why is LinkedList rarely the default choice in modern Java code, even though it has O(1) inserts?",
      "How would iterating over both compare in real-world performance?",
    ],
  },
  {
    id: 'hashmap',
    order: 21,
    tier: 'intermediate',
    title: 'HashMap',
    tagline: 'A library that shelves books by formula, not by arrival',
    expandReaction: "The most-used collection in real Java code — worth knowing exactly what it does and doesn't guarantee.",
    analogy: {
      emoji: '🗃️',
      text: "A HashMap is like a library that uses a special formula to know exactly which shelf a book goes on just from its title, so you never have to search shelf by shelf — but the shelves aren't kept in any promised order.",
    },
    explainer: "HashMap stores key-value pairs using a hash function to compute which internal 'bucket' a key belongs in, giving average O(1) lookup, insert, and remove. It does not guarantee any particular iteration order, and allows one null key.",
    codeExample: {
      label: 'In real code',
      code: `Map<String, Integer> ages = new HashMap<>();\nages.put("Amit", 23);\nages.put("Riya", 25);\nages.put("Zara", 21);\nSystem.out.println(ages.get("Riya"));\nSystem.out.println(ages); // order not guaranteed`,
      output: [
        'ages.get("Riya") → 25 (instant lookup via hashing)',
        'ages → order is not guaranteed (could print in any sequence)',
        '✓ Fast key-based lookup, but no promise about iteration order',
      ],
    },
    kaiClaim: {
      text: "So a HashMap will always print keys in the exact order I inserted them?",
      isCorrect: false,
      whyRight: "No — HashMap makes no ordering guarantee at all. If you need insertion order, that's what LinkedHashMap is for.",
    },
    quiz: [
      {
        id: 'hashmap-q1',
        question: 'What does a HashMap guarantee about the order you\'ll see when iterating over it?',
        options: ['Nothing — no particular order is guaranteed', 'Exactly the order keys were inserted', 'Alphabetical order of the keys'],
        correctIndex: 0,
        explainCorrect: "Right — HashMap trades ordering for fast, hash-based lookup.",
        explainIncorrect: "Not quite — HashMap doesn't guarantee insertion order or alphabetical order; that's not part of its contract.",
        hint: "Think about the library shelving formula again — does it shelve books in the order they arrived, or wherever the formula says?",
      },
    ],
    interviewChallenge: [
      "Why is HashMap's average lookup time O(1)?",
      "What happens when two keys produce the same hash code (a collision)?",
      "Why should HashMap keys be immutable?",
    ],
  },
  {
    id: 'linkedhashmap',
    order: 22,
    tier: 'intermediate',
    title: 'LinkedHashMap',
    tagline: 'A HashMap with a string threading through in arrival order',
    expandReaction: "The fix for the exact limitation you just saw in HashMap — good to read right after it.",
    analogy: {
      emoji: '📿',
      text: "Like a HashMap, but it also keeps a string threading through the books in the order they were placed on the shelf, so you can walk that string later and get them back in insertion order.",
    },
    explainer: "LinkedHashMap extends HashMap but additionally maintains a doubly-linked list running through all entries, preserving insertion order (or optionally access order) during iteration — at a small memory and performance cost over a plain HashMap.",
    codeExample: {
      label: 'In real code',
      code: `Map<String, Integer> ages = new LinkedHashMap<>();\nages.put("Zara", 21);\nages.put("Amit", 23);\nages.put("Riya", 25);\nSystem.out.println(ages); // {Zara=21, Amit=23, Riya=25}`,
      output: [
        'ages → {Zara=21, Amit=23, Riya=25} — exactly insertion order, every time',
        '✓ Same hashing speed as HashMap, plus a guaranteed iteration order',
      ],
    },
    kaiClaim: {
      text: "So LinkedHashMap is basically just a slower HashMap with no real benefit?",
      isCorrect: false,
      whyRight: "It is slightly slower due to the extra linked list, but the real benefit is guaranteed insertion order — a plain HashMap gives you no such guarantee at all.",
    },
    quiz: [
      {
        id: 'linkedhashmap-q1',
        question: 'What does LinkedHashMap add on top of a regular HashMap?',
        options: ['A guaranteed iteration order (usually insertion order)', 'Faster lookup than HashMap', 'Automatic sorting by key'],
        correctIndex: 0,
        explainCorrect: "Right — the linked list threading through the entries is what preserves order.",
        explainIncorrect: "Not quite — it's not faster than HashMap, and it doesn't sort keys, it just remembers the order they were added.",
        hint: "Think about the string threading through the shelved books — what does following that string give you?",
      },
    ],
    interviewChallenge: [
      "What's the actual memory cost of the extra ordering LinkedHashMap provides?",
      "Can LinkedHashMap be configured to track access order instead of insertion order? What's that useful for?",
      "When would you choose LinkedHashMap over a TreeMap if you need some kind of order?",
    ],
  },
  {
    id: 'treemap',
    order: 23,
    tier: 'intermediate',
    title: 'TreeMap',
    tagline: 'A dictionary that keeps itself alphabetized',
    expandReaction: "Pairs naturally with HashMap and LinkedHashMap — good to compare all three back to back.",
    analogy: {
      emoji: '🌳',
      text: "A TreeMap is like a dictionary that keeps itself alphabetically sorted at all times — every time you add a new word, it automatically slots into the right alphabetical position instead of being shelved wherever a formula says.",
    },
    explainer: "TreeMap stores keys in sorted order (natural ordering, or a custom Comparator), backed by a red-black tree, giving O(log n) operations instead of HashMap's average O(1) — the trade-off for guaranteed sorted order.",
    codeExample: {
      label: 'In real code',
      code: `Map<String, Integer> ages = new TreeMap<>();\nages.put("Zara", 21);\nages.put("Amit", 23);\nages.put("Riya", 25);\nSystem.out.println(ages); // {Amit=23, Riya=25, Zara=21} — sorted by key`,
      output: [
        'ages → {Amit=23, Riya=25, Zara=21} — always sorted by key, regardless of insert order',
        '✓ Slightly slower than HashMap, but always sorted',
      ],
    },
    kaiClaim: {
      text: "So TreeMap is faster than HashMap since it keeps things organized?",
      isCorrect: false,
      whyRight: "Actually the opposite — TreeMap is O(log n) per operation because it maintains sorted order, while HashMap is typically faster at O(1) but gives no ordering.",
    },
    quiz: [
      {
        id: 'treemap-q1',
        question: 'Why does TreeMap print {Amit=23, Riya=25, Zara=21} even though Zara was inserted first?',
        options: ['TreeMap always keeps keys in sorted order, regardless of insertion order', 'TreeMap remembers insertion order like LinkedHashMap', 'It printed in a random order by coincidence'],
        correctIndex: 0,
        explainCorrect: "Right — TreeMap guarantees sorted-by-key order at all times.",
        explainIncorrect: "Not quite — that's LinkedHashMap's behavior, not TreeMap's, and this isn't a coincidence — it's guaranteed sorting.",
        hint: "Think about the self-sorting dictionary again — does it care what order words were added, or where they belong alphabetically?",
      },
    ],
    interviewChallenge: [
      "Why is TreeMap's get/put O(log n) instead of HashMap's O(1)?",
      "What happens if you insert a key that doesn't implement Comparable into a TreeMap with no custom Comparator?",
      "How would you get a TreeMap to sort in descending order?",
    ],
  },
  {
    id: 'hashset',
    order: 24,
    tier: 'intermediate',
    title: 'HashSet',
    tagline: 'A raffle bin that rejects a ticket number already inside',
    expandReaction: "Good to see right after the Map trio — same hashing idea, applied to uniqueness instead of lookup.",
    analogy: {
      emoji: '🎫',
      text: "A HashSet is like a raffle bin — you can drop ticket numbers in, but if the same number is already inside, it doesn't get added again. Order doesn't matter, just that every number in there is unique.",
    },
    explainer: "HashSet stores unique elements using a hash table internally (it's actually backed by a HashMap under the hood, storing elements as keys), giving average O(1) add, contains, and remove, with no ordering guarantee.",
    codeExample: {
      label: 'In real code',
      code: `Set<String> tickets = new HashSet<>();\ntickets.add("A102");\ntickets.add("A102"); // duplicate, ignored\ntickets.add("B207");\nSystem.out.println(tickets.size()); // 2`,
      output: [
        'tickets.size() → 2 (the duplicate "A102" never actually got added twice)',
        '✓ Uniqueness enforced automatically, order not guaranteed',
      ],
    },
    kaiClaim: {
      text: "So a HashSet is secretly just a HashMap under the hood?",
      isCorrect: true,
      whyRight: "Actually yes — Java's HashSet is implemented internally using a HashMap where each element is stored as a key with a dummy value.",
    },
    quiz: [
      {
        id: 'hashset-q1',
        question: 'Why does tickets.size() come out to 2 instead of 3, even though add() was called three times?',
        options: ['The duplicate "A102" was silently rejected because Sets enforce uniqueness', 'HashSet has a maximum size limit of 2', 'The third add() call threw an exception'],
        correctIndex: 0,
        explainCorrect: "Right — HashSet just declines to add something already present, no error, no crash.",
        explainIncorrect: "Not quite — HashSet has no such size limit, and it doesn't throw when you add a duplicate, it just quietly ignores it.",
        hint: "Think about the raffle bin again — if the same ticket number gets dropped in twice, how many unique tickets are actually in there?",
      },
    ],
    interviewChallenge: [
      "What two methods must a custom class override correctly for HashSet to detect duplicates properly?",
      "What happens if you only override equals() but not hashCode()?",
      "Why is a HashSet's iteration order not something you should ever rely on?",
    ],
  },
  {
    id: 'linkedhashset',
    order: 25,
    tier: 'intermediate',
    title: 'LinkedHashSet',
    tagline: 'A HashSet that also remembers the order things arrived',
    expandReaction: "Same relationship as HashMap → LinkedHashMap, just applied to Set.",
    analogy: {
      emoji: '🔗',
      text: "Same idea as LinkedHashMap, but for uniqueness instead of key-value pairs — items still have to be unique, but LinkedHashSet also remembers the order they were first added, instead of shuffling them by hash.",
    },
    explainer: "LinkedHashSet extends HashSet, adding a linked list that preserves insertion order during iteration — it combines a Set's uniqueness guarantee with a predictable, repeatable ordering.",
    codeExample: {
      label: 'In real code',
      code: `Set<String> tags = new LinkedHashSet<>();\ntags.add("java");\ntags.add("interview");\ntags.add("java"); // duplicate, ignored\nSystem.out.println(tags); // [java, interview] — insertion order preserved`,
      output: [
        'tags → [java, interview] — in the exact order first added, duplicate skipped',
        '✓ Uniqueness like HashSet, plus guaranteed insertion order',
      ],
    },
    kaiClaim: {
      text: "So LinkedHashSet allows duplicates as long as they're kept in a nice order?",
      isCorrect: false,
      whyRight: "No — it still fully enforces uniqueness like any Set. The only thing it adds is remembering the order elements were first added.",
    },
    quiz: [
      {
        id: 'linkedhashset-q1',
        question: "What's the one thing LinkedHashSet adds on top of plain HashSet's behavior?",
        options: ['Predictable iteration order (insertion order)', 'Permission to store duplicate elements', 'Automatic sorting of elements'],
        correctIndex: 0,
        explainCorrect: "Right — same uniqueness rules as HashSet, plus a remembered insertion order.",
        explainIncorrect: "Not quite — it doesn't allow duplicates, and it doesn't sort — that's TreeSet's job.",
        hint: "Think about LinkedHashMap again — what did the 'Linked' part add there?",
      },
    ],
    interviewChallenge: [
      "Why would you pick LinkedHashSet over TreeSet if you just want predictable order but not sorting?",
      "Does LinkedHashSet add any overhead compared to plain HashSet? What is it?",
      "Can LinkedHashSet contain duplicate elements under any circumstance?",
    ],
  },
  {
    id: 'treeset',
    order: 26,
    tier: 'intermediate',
    title: 'TreeSet',
    tagline: 'A guest list that alphabetizes itself',
    expandReaction: "Rounds out the Set trio — same relationship as TreeMap has to HashMap.",
    analogy: {
      emoji: '🌲',
      text: "A TreeSet is like a guest list that keeps itself alphabetized automatically as new, unique names get added — no manual sorting ever needed.",
    },
    explainer: "TreeSet stores unique elements in sorted order (natural ordering, or via a Comparator). It's actually backed by a TreeMap internally, giving O(log n) operations with guaranteed sorted iteration.",
    codeExample: {
      label: 'In real code',
      code: `Set<Integer> scores = new TreeSet<>();\nscores.add(42);\nscores.add(7);\nscores.add(19);\nSystem.out.println(scores); // [7, 19, 42] — sorted automatically`,
      output: [
        'scores → [7, 19, 42] — always sorted ascending, regardless of insertion order',
        '✓ Uniqueness like any Set, plus guaranteed sorted order',
      ],
    },
    kaiClaim: {
      text: "So TreeSet is basically just a TreeMap wearing a different name?",
      isCorrect: true,
      whyRight: "Close enough for how it works internally — TreeSet is actually backed by a TreeMap under the hood, storing each element as a key.",
    },
    quiz: [
      {
        id: 'treeset-q1',
        question: 'Why does scores print as [7, 19, 42] even though 42 was added first?',
        options: ['TreeSet always keeps its elements sorted, regardless of insertion order', 'TreeSet remembers insertion order like LinkedHashSet', 'It sorted randomly by coincidence'],
        correctIndex: 0,
        explainCorrect: "Right — sorted order is guaranteed at all times with a TreeSet.",
        explainIncorrect: "Not quite — that's LinkedHashSet's behavior, and this isn't a coincidence, TreeSet always sorts.",
        hint: "Think about the self-alphabetizing guest list again.",
      },
    ],
    interviewChallenge: [
      "What interface must elements implement to go into a TreeSet without a custom Comparator?",
      "How would you make a TreeSet of Strings sort case-insensitively?",
      "Why is TreeSet often used when you need both uniqueness and sorted order at the same time?",
    ],
  },
  {
    id: 'queue',
    order: 27,
    tier: 'intermediate',
    title: 'Queue',
    tagline: 'First in line, first one served',
    expandReaction: "The foundation for the next two concepts — get FIFO solid before PriorityQueue and Stack.",
    analogy: {
      emoji: '🎟️',
      text: "A Queue is like a line at a ticket counter — the first person to join the line is the first one served. First in, first out (FIFO).",
    },
    explainer: "Queue is an interface representing a first-in-first-out (FIFO) collection, typically used via offer()/poll()/peek() (which return a special value on failure) rather than add()/remove() (which throw exceptions instead). Common implementations include LinkedList and ArrayDeque.",
    codeExample: {
      label: 'In real code',
      code: `Queue<String> line = new LinkedList<>();\nline.offer("Amit");\nline.offer("Riya");\nline.offer("Zara");\nSystem.out.println(line.poll()); // Amit — first one in\nSystem.out.println(line.poll()); // Riya — next in line`,
      output: [
        'line.poll() → "Amit" (the first person who joined the line)',
        'line.poll() → "Riya" (next in line)',
        '✓ First in, first out — exactly like a real line',
      ],
    },
    kaiClaim: {
      text: "So a Queue serves whoever joined most recently, like a stack of plates?",
      isCorrect: false,
      whyRight: "No — that's a Stack (LIFO). A Queue serves whoever has been waiting longest — first in, first out.",
    },
    quiz: [
      {
        id: 'queue-q1',
        question: "In the code above, why does the first poll() return 'Amit' and not 'Zara'?",
        options: ['Queue is FIFO — the first element added is the first one removed', 'Queue always returns elements in alphabetical order', 'poll() removes a random element'],
        correctIndex: 0,
        explainCorrect: "Right — FIFO means the earliest arrival leaves first, just like a real line.",
        explainIncorrect: "Not quite — there's no alphabetical sorting or randomness here, poll() always returns the head of the queue (the oldest entry).",
        hint: "Think about the ticket counter line again — who gets served first, the first person in line or the last?",
      },
    ],
    interviewChallenge: [
      "What's the difference between add()/remove() and offer()/poll() when a queue operation can't succeed?",
      "Why would offer() be preferred over add() in production code?",
      "Which concrete classes commonly implement the Queue interface?",
    ],
  },
  {
    id: 'priorityqueue',
    order: 28,
    tier: 'intermediate',
    title: 'PriorityQueue',
    tagline: 'An ER room, not a first-come line',
    expandReaction: "A great contrast right after Queue — same interface, completely different ordering rule.",
    analogy: {
      emoji: '🚑',
      text: "An ER waiting room isn't first-come-first-served — the most critical patient gets seen first, regardless of arrival order. A PriorityQueue works the same way: the highest-priority element always comes out first, not the one that arrived first.",
    },
    explainer: "PriorityQueue is a queue where elements come out ordered by priority (natural ordering, or a Comparator) rather than arrival order — the smallest (or highest-priority) element is always at the head, ready to be polled first.",
    codeExample: {
      label: 'In real code',
      code: `Queue<Integer> urgent = new PriorityQueue<>();\nurgent.offer(5);\nurgent.offer(1);\nurgent.offer(3);\nSystem.out.println(urgent.poll()); // 1 — smallest first, not the first one added\nSystem.out.println(urgent.poll()); // 3`,
      output: [
        'urgent.poll() → 1 (smallest value, even though 5 was added first)',
        'urgent.poll() → 3',
        '✓ Priority order wins over arrival order',
      ],
    },
    kaiClaim: {
      text: "So PriorityQueue still returns elements in the order they were added, just like a regular Queue?",
      isCorrect: false,
      whyRight: "No — that's the whole point of the difference. PriorityQueue ignores arrival order and always returns the smallest (or highest-priority) element first.",
    },
    quiz: [
      {
        id: 'priorityqueue-q1',
        question: 'Why does urgent.poll() return 1 first, even though 5 was added before it?',
        options: ['PriorityQueue always returns the smallest / highest-priority element first, not by arrival order', 'PriorityQueue is broken and returning elements randomly', 'Numbers under 5 always get priority in Java'],
        correctIndex: 0,
        explainCorrect: "Right — priority (natural ordering here) decides the order, not arrival time.",
        explainIncorrect: "Not quite — this isn't random or a special rule about the number 5, it's the general behavior of a PriorityQueue: smallest/highest-priority comes out first.",
        hint: "Think about the ER waiting room again — does the most urgent patient wait for their turn in line?",
      },
    ],
    interviewChallenge: [
      "What happens if you add elements to a PriorityQueue that don't implement Comparable and no Comparator is given?",
      "Is a PriorityQueue's iterator guaranteed to return elements in priority order?",
      "What's the time complexity of poll() on a PriorityQueue, and why?",
    ],
  },
  {
    id: 'stack',
    order: 29,
    tier: 'intermediate',
    title: 'Stack',
    tagline: 'The top plate comes off first',
    expandReaction: "The natural counterpart to Queue — LIFO vs FIFO, worth contrasting directly.",
    analogy: {
      emoji: '🍽️',
      text: "A stack of plates — you can only take the top plate off, and any new plate goes on top too. Last in, first out (LIFO).",
    },
    explainer: "Stack is a LIFO (last-in-first-out) collection — push() adds to the top, pop() removes from the top, peek() looks at the top without removing it. In modern Java, ArrayDeque is often recommended over the legacy Stack class for this purpose.",
    codeExample: {
      label: 'In real code',
      code: `Stack<String> plates = new Stack<>();\nplates.push("plate1");\nplates.push("plate2");\nplates.push("plate3");\nSystem.out.println(plates.pop()); // plate3 — the one placed most recently\nSystem.out.println(plates.pop()); // plate2`,
      output: [
        'plates.pop() → "plate3" (the most recently added plate)',
        'plates.pop() → "plate2"',
        '✓ Last in, first out — exactly like a stack of plates',
      ],
    },
    kaiClaim: {
      text: "So a Stack removes the oldest item first, same as a Queue?",
      isCorrect: false,
      whyRight: "No — that's the opposite. A Stack is LIFO (last in, first out); a Queue is FIFO (first in, first out).",
    },
    quiz: [
      {
        id: 'stack-q1',
        question: "Why does plates.pop() return 'plate3' first, even though it was added last?",
        options: ['Stack is LIFO — the most recently added item comes off first', 'Stack always removes items alphabetically', 'pop() removes a random plate'],
        correctIndex: 0,
        explainCorrect: "Right — LIFO means the last one in is the first one out, just like the top plate in a stack.",
        explainIncorrect: "Not quite — there's no alphabetical order or randomness, pop() always removes the most recently pushed item.",
        hint: "Think about a real stack of plates — which plate do you take off first, the top one or the bottom one?",
      },
    ],
    interviewChallenge: [
      "Why is ArrayDeque generally recommended over the legacy Stack class today?",
      "What happens if you call pop() on an empty Stack?",
      "How would you implement a Stack using two Queues?",
    ],
  },
  {
    id: 'deque',
    order: 30,
    tier: 'intermediate',
    title: 'Deque',
    tagline: 'A line you can join or leave from either end',
    expandReaction: "Ties Queue and Stack together — worth seeing right after both.",
    analogy: {
      emoji: '🎡',
      text: "A deque (double-ended queue) is like a line where people can join or leave from either end — front or back — unlike a normal single-direction line.",
    },
    explainer: "Deque (double-ended queue) supports adding and removing elements from both the front and the back, so it can act as either a Queue (FIFO) or a Stack (LIFO) depending on which end you use. ArrayDeque is a common, efficient implementation.",
    codeExample: {
      label: 'In real code',
      code: `Deque<String> line = new ArrayDeque<>();\nline.addFirst("frontPerson");\nline.addLast("backPerson");\nSystem.out.println(line.peekFirst()); // frontPerson\nSystem.out.println(line.peekLast());  // backPerson`,
      output: [
        'line.peekFirst() → "frontPerson"',
        'line.peekLast() → "backPerson"',
        '✓ Elements can be added or viewed from either end',
      ],
    },
    kaiClaim: {
      text: "So a Deque can only be used as a Queue, never as a Stack?",
      isCorrect: false,
      whyRight: "No — a Deque can act as either. Use one end for Queue-like FIFO behavior, or just the front for Stack-like LIFO behavior.",
    },
    quiz: [
      {
        id: 'deque-q1',
        question: 'What makes a Deque different from a regular Queue?',
        options: ['You can add and remove elements from both ends, not just one', 'A Deque can only hold numbers', 'A Deque automatically sorts its elements'],
        correctIndex: 0,
        explainCorrect: "Right — double-ended access is exactly what 'Deque' stands for.",
        explainIncorrect: "Not quite — Deque isn't limited to numbers, and it doesn't sort anything. The key feature is access from both ends.",
        hint: "The name 'Deque' is short for 'double-ended queue' — what does that suggest about which ends you can use?",
      },
    ],
    interviewChallenge: [
      "How would you implement both a Stack and a Queue using a single Deque?",
      "What's the difference between ArrayDeque and LinkedList when both implement Deque?",
      "Can a Deque contain null elements? Why or why not?",
    ],
  },
  {
    id: 'iterator',
    order: 31,
    tier: 'intermediate',
    title: 'Iterator',
    tagline: 'A bookmark that walks the pages one at a time',
    expandReaction: "Small interface, but understanding it explains a very common real-world crash.",
    analogy: {
      emoji: '🔍',
      text: "An Iterator is like a bookmark that walks through a book one page at a time, letting you check 'is there a next page?' and 'give me the next page' — without needing to know how many pages are ahead.",
    },
    explainer: "Iterator provides a uniform way to traverse any collection one element at a time via hasNext()/next(), without exposing the collection's internal structure. It also supports safely removing elements during iteration via remove() — something a plain for-each loop can't do without risking a ConcurrentModificationException.",
    codeExample: {
      label: 'In real code',
      code: `List<String> names = new ArrayList<>(List.of("Amit", "Riya", "Zara"));\nIterator<String> it = names.iterator();\nwhile (it.hasNext()) {\n  String name = it.next();\n  if (name.equals("Riya")) it.remove(); // safe removal during iteration\n}\nSystem.out.println(names); // [Amit, Zara]`,
      output: [
        'it.hasNext() / it.next() walk through each name',
        'it.remove() safely deletes "Riya" mid-iteration',
        'names → [Amit, Zara]',
        "✓ Safe removal during traversal, which a plain for-each loop can't do",
      ],
    },
    kaiClaim: {
      text: "So I could do the same safe removal using a regular for-each loop instead of an Iterator?",
      isCorrect: false,
      whyRight: "No — removing from a collection during a for-each loop throws a ConcurrentModificationException. The Iterator's own remove() method is specifically built to do this safely.",
    },
    quiz: [
      {
        id: 'iterator-q1',
        question: 'Why use it.remove() instead of names.remove(name) while looping with the Iterator?',
        options: ['it.remove() safely removes during iteration; modifying the list directly mid-loop can throw ConcurrentModificationException', "There's no real difference between the two", 'names.remove(name) is actually faster'],
        correctIndex: 0,
        explainCorrect: "Right — the Iterator's remove() is aware of the iteration state and keeps things consistent.",
        explainIncorrect: "Not quite — there is a real difference, and speed isn't the issue here — safety during iteration is.",
        hint: "Think about the bookmark again — if you rip a page out of the book while someone else is flipping through it by hand, what could go wrong?",
      },
    ],
    interviewChallenge: [
      "What actually throws ConcurrentModificationException, and when does the check happen?",
      "What's the difference between Iterator and ListIterator?",
      "Why doesn't a for-each loop let you safely remove elements mid-iteration?",
    ],
  },
  {
    id: 'comparable',
    order: 32,
    tier: 'intermediate',
    title: 'Comparable',
    tagline: "A class's own built-in way to line up",
    expandReaction: "Pairs directly with the next concept — the difference between the two is a classic interview question.",
    analogy: {
      emoji: '✍️',
      text: "Comparable is like giving every person a default way to line up — for example, always by height, built right into who they are.",
    },
    explainer: "A class implements Comparable<T> and defines compareTo() to establish its own single, natural/default ordering. This is the class's own built-in opinion about how its instances should sort against each other — Collections.sort() uses it automatically if no other ordering is given.",
    codeExample: {
      label: 'In real code',
      code: `class Person implements Comparable<Person> {\n  String name; int age;\n  Person(String name, int age) { this.name = name; this.age = age; }\n  public int compareTo(Person other) { return this.age - other.age; } // natural order: by age\n}\n\nList<Person> people = new ArrayList<>(List.of(new Person("Riya", 25), new Person("Amit", 23)));\nCollections.sort(people); // uses compareTo() automatically`,
      output: [
        "Collections.sort(people) uses Person's own compareTo()",
        "Result order: Amit (23), then Riya (25) — sorted by age, the class's natural order",
        "✓ The class defines its own single default sort order",
      ],
    },
    kaiClaim: {
      text: "So Comparable lets me define multiple different sort orders for the same class?",
      isCorrect: false,
      whyRight: "No — Comparable gives a class exactly one natural ordering via compareTo(). For multiple different sort orders, you'd use Comparator instead.",
    },
    quiz: [
      {
        id: 'comparable-q1',
        question: 'What does implementing Comparable<Person> and compareTo() give the Person class?',
        options: ['A single natural/default sort order used automatically by things like Collections.sort()', 'Multiple different sort orders to choose from', 'Automatic equality checking (equals())'],
        correctIndex: 0,
        explainCorrect: "Right — Comparable defines one built-in ordering for the class itself.",
        explainIncorrect: "Not quite — multiple orderings is Comparator's job, and compareTo() isn't the same as equals().",
        hint: "Think about the height-based lineup again — is that one built-in rule, or several different ways to line up?",
      },
    ],
    interviewChallenge: [
      "What does compareTo() need to return, and what do positive/negative/zero each mean?",
      "Can a class only ever have one compareTo() implementation?",
      "What breaks if compareTo() is inconsistent with equals()?",
    ],
  },
  {
    id: 'comparator',
    order: 33,
    tier: 'intermediate',
    title: 'Comparator',
    tagline: "A custom rulebook, handed to the referee for just today",
    expandReaction: "The direct answer to Comparable's one limitation — read these two back to back.",
    analogy: {
      emoji: '🧮',
      text: "A Comparator is like handing a referee a custom rulebook for just this one race — 'actually, for today's race, line them up by age instead of height' — without changing anything about the runners themselves.",
    },
    explainer: "A Comparator<T> is a separate object that defines a custom ordering, independent of the class itself. You can write as many different Comparators as you need (by name, by age, reversed, etc.) and pass whichever one you want into sort(), without ever touching the class's own compareTo().",
    codeExample: {
      label: 'In real code',
      code: `List<Person> people = new ArrayList<>(List.of(new Person("Riya", 25), new Person("Amit", 23)));\n\nComparator<Person> byName = (a, b) -> a.name.compareTo(b.name);\npeople.sort(byName); // sorted by name this time, ignoring Person's own compareTo()`,
      output: [
        'people.sort(byName) → sorted by name: Amit, then Riya',
        "✓ A completely custom order, defined outside the Person class itself",
      ],
    },
    kaiClaim: {
      text: "So I'd have to modify the Person class every time I want to sort it a different way?",
      isCorrect: false,
      whyRight: "No — that's exactly what Comparator avoids. You can write a new Comparator for any ordering you want without ever touching the Person class.",
    },
    quiz: [
      {
        id: 'comparator-q1',
        question: "Why would you use a Comparator instead of relying on Person's compareTo()?",
        options: ['When you want a different, custom sort order without changing the class itself', 'Comparator is required before you can use any List', "compareTo() doesn't actually work in Java"],
        correctIndex: 0,
        explainCorrect: "Right — Comparator lets you sort however you want, as many ways as you need, from outside the class.",
        explainIncorrect: "Not quite — Lists work fine without a Comparator, and compareTo() works perfectly well on its own for the natural order.",
        hint: "Think about the referee's custom rulebook again — does it change anything about the runners themselves?",
      },
    ],
    interviewChallenge: [
      "How would you chain two Comparators together, e.g. sort by age, then by name?",
      "What's the difference between Comparator.comparing() and writing your own compare() method?",
      "Can the same class have several different Comparators defined for it?",
    ],
  },
  {
    id: 'fail-fast-vs-fail-safe',
    order: 34,
    tier: 'advanced',
    title: 'Fail-Fast vs Fail-Safe',
    tagline: 'A fire alarm vs inspecting a photograph of the room',
    expandReaction: "This explains a real crash you'll actually hit in production — worth understanding, not memorizing.",
    analogy: {
      emoji: '🚨',
      text: "Fail-fast is like a fire alarm that instantly shrieks the moment something changes mid-check — it won't let you keep inspecting a room that's actively being rearranged. Fail-safe is like inspecting a photograph of the room instead — even if someone rearranges the real room while you look at the photo, your inspection just continues undisturbed.",
    },
    explainer: "Fail-fast iterators (like ArrayList's or HashMap's) throw a ConcurrentModificationException immediately if the collection is structurally modified while iterating, by checking an internal modification counter. Fail-safe iterators (like those from ConcurrentHashMap) work on a separate snapshot or the live concurrent structure instead, so concurrent modification doesn't throw — though the iteration might not reflect the very latest changes.",
    codeExample: {
      label: 'In real code',
      code: `List<String> list = new ArrayList<>(List.of("a", "b", "c"));\nfor (String s : list) {\n  list.remove(s); // modifying while iterating — fail-fast\n} // throws ConcurrentModificationException\n\nMap<String, Integer> safeMap = new ConcurrentHashMap<>();\nsafeMap.put("a", 1);\nfor (String key : safeMap.keySet()) {\n  safeMap.put("b", 2); // modifying while iterating — fail-safe, no exception\n}`,
      output: [
        'ArrayList loop → 💥 ConcurrentModificationException (fail-fast)',
        'ConcurrentHashMap loop → runs fine, no exception (fail-safe)',
        '✓ Same idea (modifying during iteration), very different outcome depending on the collection',
      ],
    },
    kaiClaim: {
      text: "So fail-safe means the collection just quietly ignores any modification attempt during iteration?",
      isCorrect: false,
      whyRight: "No — fail-safe collections don't ignore the modification, they actually apply it. They just don't throw an exception, because the iterator isn't tied to the exact same live structure the way a fail-fast one is.",
    },
    quiz: [
      {
        id: 'fail-fast-vs-fail-safe-q1',
        question: 'Why does modifying an ArrayList during a for-each loop throw an exception, but modifying a ConcurrentHashMap the same way doesn\'t?',
        options: ["ArrayList's iterator is fail-fast and detects the change immediately; ConcurrentHashMap's iterator is fail-safe and tolerates it", 'ConcurrentHashMap silently discards all modification attempts', 'ArrayList is simply buggy compared to ConcurrentHashMap'],
        correctIndex: 0,
        explainCorrect: "Right — it's a deliberate design difference in how each iterator handles concurrent structural changes.",
        explainIncorrect: "Not quite — ConcurrentHashMap doesn't discard the change (it does apply it), and ArrayList isn't buggy, it's intentionally strict.",
        hint: "Think about the fire alarm vs the photograph again — which one reacts instantly, and which one just keeps working from what it already captured?",
      },
    ],
    interviewChallenge: [
      "Why does a fail-safe iterator not always reflect the very latest state of the collection?",
      "Give an example of a fail-safe collection and a fail-fast one.",
      "Is CopyOnWriteArrayList fail-fast or fail-safe, and why?",
    ],
  },
  {
    id: 'concurrenthashmap',
    order: 35,
    tier: 'advanced',
    title: 'ConcurrentHashMap',
    tagline: 'A ledger split into sections, each with its own lock',
    expandReaction: "Last one — this ties directly back to fail-safe behavior, so the two reinforce each other.",
    analogy: {
      emoji: '🧵',
      text: "A regular HashMap is like one single ledger book that only one clerk can safely write in at a time — two clerks writing simultaneously can corrupt the page. A ConcurrentHashMap is like a ledger split into many separate sections, each with its own lock, so multiple clerks can safely write to different sections at the same time.",
    },
    explainer: "ConcurrentHashMap is a thread-safe map designed for concurrent access, achieving much better throughput than a plain HashMap wrapped in synchronization by only locking small internal segments or buckets instead of the entire map on every write. Its iterators are fail-safe rather than fail-fast, tolerating concurrent modification without throwing.",
    codeExample: {
      label: 'In real code',
      code: `Map<String, Integer> counts = new ConcurrentHashMap<>();\ncounts.put("visits", 0);\n\n// Two threads can safely update different keys at the same time\ncounts.merge("visits", 1, Integer::sum); // thread-safe increment`,
      output: [
        'counts.merge("visits", 1, Integer::sum) → safely increments even under concurrent access',
        '✓ Multiple threads can read/write without external synchronization, and without full-map locking',
      ],
    },
    kaiClaim: {
      text: "So ConcurrentHashMap works by locking the entire map on every single operation, just like a synchronized HashMap?",
      isCorrect: false,
      whyRight: "No — that's the whole reason it exists. ConcurrentHashMap locks only small internal portions, not the whole map, which is exactly why it performs so much better than a fully synchronized HashMap under concurrent load.",
    },
    quiz: [
      {
        id: 'concurrenthashmap-q1',
        question: 'Why does ConcurrentHashMap generally perform better than a HashMap wrapped in Collections.synchronizedMap() under heavy concurrent use?',
        options: ['It locks only small internal portions of the map instead of the whole thing on every operation', 'It secretly disables thread safety to go faster', "It's actually not thread-safe at all"],
        correctIndex: 0,
        explainCorrect: "Right — fine-grained locking is exactly what gives it better concurrent throughput.",
        explainIncorrect: "Not quite — it doesn't disable thread safety, and it absolutely is thread-safe; the performance gain comes from smarter, more granular locking.",
        hint: "Think about the ledger with separate locked sections again — does every clerk have to wait for the same single lock?",
      },
    ],
    interviewChallenge: [
      "How does ConcurrentHashMap avoid locking the entire map on every write?",
      "Does ConcurrentHashMap allow null keys or null values? Why might that design choice exist?",
      "Why might size() on a ConcurrentHashMap be approximate under heavy concurrent writes?",
    ],
  },
  {
    id: 'jvm',
    order: 36,
    tier: 'advanced',
    title: 'JVM',
    tagline: 'The universal adapter that lets one plug fit any socket',
    expandReaction: "This is the answer to 'why does Java run everywhere' — worth knowing precisely, not just the buzzword.",
    analogy: {
      emoji: '🖥️',
      text: "The JVM is like a universal power adapter — you plug the same appliance (your compiled program) into it, and it figures out how to make that work with whatever socket (operating system) it's actually connected to. That's what 'write once, run anywhere' really means.",
    },
    explainer: "The Java Virtual Machine (JVM) is the engine that actually executes Java bytecode, translating it into instructions the underlying operating system and hardware understand. Since every platform (Windows, Mac, Linux) has its own JVM implementation, the exact same compiled bytecode runs unchanged on all of them.",
    codeExample: {
      label: 'In real code',
      code: `// javac compiles Java source into portable bytecode\njavac Hello.java   // -> Hello.class\n\n// java launches the JVM, which reads and runs the bytecode\njava Hello          // JVM translates bytecode into real machine instructions`,
      output: [
        'javac Hello.java → produces Hello.class (portable bytecode)',
        'java Hello → the JVM interprets/compiles that bytecode for THIS machine',
        '✓ The same Hello.class runs unchanged on Windows, Mac, or Linux — each has its own JVM',
      ],
    },
    kaiClaim: {
      text: "So the JVM is just another name for the Java compiler?",
      isCorrect: false,
      whyRight: "No — javac is the compiler, turning source into bytecode. The JVM is what actually runs that bytecode afterward, on whatever machine it's on.",
    },
    quiz: [
      {
        id: 'jvm-q1',
        question: 'What does the JVM actually do with Hello.class?',
        options: ["Translates and executes the portable bytecode for the actual machine it's running on", 'Compiles Hello.java directly into machine code', 'Stores Hello.class permanently on a server'],
        correctIndex: 0,
        explainCorrect: "Right — the JVM's whole job is running bytecode on the machine it's installed on.",
        explainIncorrect: "Not quite — that's the compiler's job (turning source into bytecode), and the JVM doesn't store anything on a server.",
        hint: "Think about the universal adapter again — what does it actually do once it's plugged in?",
      },
    ],
    interviewChallenge: [
      "What's the difference between \"the JVM\" and a specific JVM implementation like HotSpot?",
      "Why can the exact same .class file behave identically on Windows and Linux?",
      "Does the JVM interpret bytecode, compile it just-in-time, or both?",
    ],
  },
  {
    id: 'jre',
    order: 37,
    tier: 'advanced',
    title: 'JRE',
    tagline: 'The appliance and adapter, but no workshop tools',
    expandReaction: "Sets up the JVM → JRE → JDK ladder — read these three together.",
    analogy: {
      emoji: '📦',
      text: "The JRE is like buying the appliance plus its power adapter (the JVM) and a starter set of standard tools you need to actually use it — but no tools to build or repair a new appliance yourself.",
    },
    explainer: "The Java Runtime Environment (JRE) bundles the JVM with the core Java class libraries (java.lang, java.util, java.io, and so on) needed to run Java applications. It does not include development tools like the compiler (javac) — it's built for running Java programs, not writing or building new ones.",
    codeExample: {
      label: 'In real code',
      code: `// JRE contains:\n//   - the JVM (to run bytecode)\n//   - core libraries (java.lang, java.util, java.io, etc.)\n// JRE does NOT contain:\n//   - javac (the compiler)`,
      output: [
        'JRE = JVM + standard libraries',
        'Can run: java Hello ✓',
        'Cannot do: javac Hello.java ✗ (compiler isn\'t included)',
        '✓ JRE is for running Java apps, not building them',
      ],
    },
    kaiClaim: {
      text: "So I can compile my own Java source files with just a JRE installed?",
      isCorrect: false,
      whyRight: "No — the JRE doesn't include javac. For compiling, you need the JDK, which includes the JRE plus the development tools.",
    },
    quiz: [
      {
        id: 'jre-q1',
        question: 'What can you NOT do with only the JRE installed (no JDK)?',
        options: ['Compile a .java source file into bytecode', 'Run an already-compiled .class file', 'Use core libraries like java.util'],
        correctIndex: 0,
        explainCorrect: "Right — compiling requires javac, which lives in the JDK, not the JRE.",
        explainIncorrect: "Not quite — running .class files and using core libraries both work fine with just the JRE. It's compiling that's missing.",
        hint: "Think about the appliance-plus-adapter again — does that box include a workshop to build a new appliance?",
      },
    ],
    interviewChallenge: [
      "If you only need to run a Java app in production, do you need the full JDK?",
      "What's actually inside the JRE besides the JVM?",
      "Why did later JDK distributions stop shipping a separate JRE download?",
    ],
  },
  {
    id: 'jdk',
    order: 38,
    tier: 'advanced',
    title: 'JDK',
    tagline: 'The full workshop — parts, adapter, and the toolkit',
    expandReaction: "Last one in the ladder — JVM runs it, JRE bundles what's needed to run it, JDK adds what's needed to build it.",
    analogy: {
      emoji: '🧰',
      text: "The JDK is like the full workshop — the appliance and adapter (JVM), the parts bin (libraries), and the toolkit and manuals to actually build brand new appliances yourself (compiler, debugger, docs tools).",
    },
    explainer: "The Java Development Kit (JDK) bundles the JRE (JVM + libraries) together with development tools — javac (the compiler), javadoc, jdb (the debugger), and more. If you're writing and compiling Java code, you need the JDK; if you're only running an already-compiled program, the JRE alone used to be sufficient.",
    codeExample: {
      label: 'In real code',
      code: `// JDK contains:\n//   - the JRE (JVM + libraries)\n//   - javac (compiler)\n//   - javadoc, jdb, and other dev tools\n\njavac Hello.java   // requires the JDK's compiler\njava Hello          // works with the JRE alone (or the JDK, since it includes one)`,
      output: [
        'JDK = JRE + development tools (compiler, debugger, etc.)',
        'javac requires the JDK',
        'java only requires the JRE (or the JDK, which includes it)',
        '✓ JDK is the full toolkit: build AND run. JRE is just: run.',
      ],
    },
    kaiClaim: {
      text: "So JDK, JRE, and JVM are basically three names for the exact same thing?",
      isCorrect: false,
      whyRight: "No — they're nested, not identical: the JVM runs bytecode, the JRE is the JVM plus libraries to run programs, and the JDK is the JRE plus tools to also build programs.",
    },
    quiz: [
      {
        id: 'jdk-q1',
        question: 'Which one do you need installed to compile a .java file with javac?',
        options: ['The JDK', 'The JRE alone is enough', 'The JVM alone is enough'],
        correctIndex: 0,
        explainCorrect: "Right — javac only ships with the JDK, not the JRE or a bare JVM.",
        explainIncorrect: "Not quite — neither the JRE nor a bare JVM includes the compiler. Only the JDK does.",
        hint: "Think about the full workshop again — does the bare appliance-and-adapter box include a toolkit to build new ones?",
      },
    ],
    interviewChallenge: [
      "What's the practical difference between what a developer needs vs what a production server needs?",
      "Name two tools bundled in the JDK besides javac.",
      "Why would a Docker image for running (not building) a Java app often use a JRE-only base instead of a full JDK?",
    ],
  },
  {
    id: 'garbage-collection',
    order: 39,
    tier: 'advanced',
    title: 'Garbage Collection',
    tagline: 'A cleaning crew that clears out anything nobody is using',
    expandReaction: "This is the mechanism behind why Java code never calls free() — worth knowing how it decides what to clean.",
    analogy: {
      emoji: '🧹',
      text: "Garbage collection is like a cleaning crew that automatically walks through a house after a party and throws out anything nobody is using or referencing anymore, so you never have to remember to clean it up yourself.",
    },
    explainer: "Garbage collection is the JVM automatically reclaiming heap memory occupied by objects that are no longer reachable from any active reference, so the program never has to manually free memory (unlike C/C++). Modern JVMs use generational collectors (like G1GC) that separate young and old objects for efficiency, since most objects die young.",
    codeExample: {
      label: 'In real code',
      code: `Person p = new Person("Amit"); // object created on the heap\np = null; // no more references point to the Person object\n// at some point, GC notices nothing references it and reclaims the memory`,
      output: [
        'new Person("Amit") → object lives on the heap',
        'p = null → the object becomes unreachable (nothing points to it anymore)',
        'GC eventually reclaims that memory automatically',
        '✓ You never called "free" or "delete" — the JVM cleaned it up for you',
      ],
    },
    kaiClaim: {
      text: "So garbage collection means Java immediately deletes an object the instant its reference is set to null?",
      isCorrect: false,
      whyRight: "No — setting a reference to null just makes the object eligible for collection. The actual cleanup happens later, whenever the garbage collector decides to run.",
    },
    quiz: [
      {
        id: 'garbage-collection-q1',
        question: 'What makes an object eligible for garbage collection?',
        options: ['No live references anywhere in the program point to it anymore', 'The programmer calls a delete() method on it', 'The object has existed for more than 5 seconds'],
        correctIndex: 0,
        explainCorrect: "Right — reachability is what decides eligibility, not any delete() call or a fixed timer.",
        explainIncorrect: "Not quite — Java has no delete() for objects, and there's no fixed-time rule. It's purely about whether anything still references it.",
        hint: "Think about the cleaning crew again — what do they actually throw out?",
      },
    ],
    interviewChallenge: [
      "Why do most GC pauses come from minor (young-gen) collections rather than major ones?",
      "What's the difference between a minor GC and a full GC?",
      "Can you force garbage collection to run immediately in Java? Should you?",
    ],
  },
  {
    id: 'heap-memory',
    order: 40,
    tier: 'advanced',
    title: 'Heap Memory',
    tagline: 'The shared warehouse where every object actually lives',
    expandReaction: "Pairs directly with garbage collection — this is the memory region GC is cleaning up.",
    analogy: {
      emoji: '🏗️',
      text: "The heap is like a big shared warehouse where every object gets built and stored, reachable from anywhere in the program as long as someone still holds the storage ticket (a reference) to find it.",
    },
    explainer: "Heap memory is where every object created with `new` actually lives in Java. It's shared across the entire application and its threads, managed by the garbage collector, and its size can be configured (for example, with -Xmx). Running out of heap space throws OutOfMemoryError.",
    codeExample: {
      label: 'In real code',
      code: `Person p1 = new Person("Amit"); // object stored in the heap\nPerson p2 = new Person("Riya"); // another separate object in the heap\n// p1 and p2 are references (typically on the stack) pointing into heap memory`,
      output: [
        'new Person(...) → allocates actual object storage in the heap',
        'p1, p2 → just references pointing at those heap locations',
        '✓ The objects themselves live in shared heap memory, reachable from anywhere holding a valid reference',
      ],
    },
    kaiClaim: {
      text: "So variables like p1 and p2 themselves live inside the heap?",
      isCorrect: false,
      whyRight: "Not exactly — p1 and p2 are references, typically stored on the stack as local variables. It's the actual Person objects they point to that live in the heap.",
    },
    quiz: [
      {
        id: 'heap-memory-q1',
        question: 'Where does the actual Person object created by `new Person(...)` get stored?',
        options: ['In heap memory, shared across the whole application', 'Inside the variable name itself', 'In the JDK installation folder'],
        correctIndex: 0,
        explainCorrect: "Right — the object's real storage is heap memory, shared across the whole application.",
        explainIncorrect: "Not quite — a variable name is just a reference, not storage, and objects have nothing to do with the JDK installation folder.",
        hint: "Think about the warehouse analogy again — where do the actual goods get stored?",
      },
    ],
    interviewChallenge: [
      "What JVM flag controls the maximum heap size, and what happens if you exceed it?",
      "Why is heap memory shared across threads while stack memory isn't?",
      "How would a memory leak show up in a heap dump?",
    ],
  },
  {
    id: 'stack-memory',
    order: 41,
    tier: 'advanced',
    title: 'Stack Memory',
    tagline: "A tray added for each method call, removed when it's done",
    expandReaction: "Not to be confused with the Stack collection class — same word, completely different thing, worth being precise here.",
    analogy: {
      emoji: '📚',
      text: "Stack memory is like a stack of cafeteria trays specifically for tracking which method call you're currently inside — every time you call a new method, a new tray (a stack frame) gets added on top holding its local variables; when the method finishes, that tray gets removed, revealing exactly where you were before.",
    },
    explainer: "Stack memory holds method call frames — each frame contains the local variables and partial results for one method invocation. Each thread has its own separate stack. It's fast and automatically managed, with frames pushed on call and popped on return, but limited in size — deep or infinite recursion causes a StackOverflowError.",
    codeExample: {
      label: 'In real code',
      code: `void methodA() {\n  int x = 5; // lives in methodA's stack frame\n  methodB();\n}\nvoid methodB() {\n  int y = 10; // lives in methodB's own stack frame, on top of methodA's\n}`,
      output: [
        "methodA() called → its frame (with x) pushed onto the stack",
        "methodB() called from inside → its frame (with y) pushed on top",
        "methodB() returns → its frame is popped, methodA's frame is back on top",
        "✓ Each method call gets its own frame, cleaned up automatically when it returns",
      ],
    },
    kaiClaim: {
      text: "So this 'stack' is the same thing as the java.util.Stack collection class?",
      isCorrect: false,
      whyRight: "No, unrelated despite the shared name — this stack is the JVM's internal call-stack mechanism for tracking method calls, not the Stack<E> collection class you push and pop objects from in your own code.",
    },
    quiz: [
      {
        id: 'stack-memory-q1',
        question: "What happens to methodB's stack frame the moment methodB finishes running?",
        options: ['It gets automatically popped off the stack', 'It stays on the stack forever', 'It moves into heap memory permanently'],
        correctIndex: 0,
        explainCorrect: "Right — a frame is popped the instant its method returns, revealing the caller's frame again.",
        explainIncorrect: "Not quite — frames don't persist forever, and they don't relocate to the heap. They're simply popped off.",
        hint: "Think about the cafeteria tray analogy again — what happens once you're done with your tray?",
      },
    ],
    interviewChallenge: [
      "What causes a StackOverflowError, and how does it relate to recursion?",
      "Why does each thread get its own separate stack?",
      "Is stack memory generally faster or slower to allocate than heap memory, and why?",
    ],
  },
  {
    id: 'metaspace',
    order: 42,
    tier: 'advanced',
    title: 'Metaspace',
    tagline: "The blueprint shelf, shared by every object made from it",
    expandReaction: "Comes up whenever an app loads a huge number of classes dynamically — worth knowing what actually lives here.",
    analogy: {
      emoji: '🗄️',
      text: "Metaspace is like a separate reference shelf in a library that holds the actual blueprints (class definitions) themselves, not the books (objects) made from them — every unique class loaded gets its blueprint stored here once, shared by every object made from it.",
    },
    explainer: "Metaspace (introduced in Java 8, replacing the old PermGen) stores class metadata — the structure of loaded classes, like method bytecode and field information — not object instances. Unlike PermGen, it's allocated from native (off-heap) memory and grows dynamically by default, which reduced a whole class of OutOfMemoryError: PermGen space problems from older Java versions.",
    codeExample: {
      label: 'In real code',
      code: `// When a class is first loaded/used, its structure goes into Metaspace:\nclass Person { String name; void greet() { /* ... */ } }\n\nPerson p1 = new Person(); // Person's blueprint lives in Metaspace (loaded once)\nPerson p2 = new Person(); // p2's data lives in the heap, reusing the same class metadata`,
      output: [
        "Person's class structure (fields, methods) → stored once in Metaspace",
        'p1 and p2 objects themselves → stored separately in the heap',
        '✓ Many objects, one shared class definition in Metaspace',
      ],
    },
    kaiClaim: {
      text: "So Metaspace stores the actual Person objects, just like the heap does?",
      isCorrect: false,
      whyRight: "No — Metaspace stores class-level metadata, the blueprint, not individual object instances. Actual objects always live in the heap.",
    },
    quiz: [
      {
        id: 'metaspace-q1',
        question: 'What does Metaspace actually store?',
        options: ['Class metadata — the structure of loaded classes, not individual objects', 'The actual Person objects created at runtime', 'Local variables inside method calls'],
        correctIndex: 0,
        explainCorrect: "Right — Metaspace holds class blueprints, shared by every object made from that class.",
        explainIncorrect: "Not quite — objects live in the heap and local variables live in stack frames; Metaspace is specifically for class structure.",
        hint: "Think about the blueprint shelf again — is it storing finished products, or the plans used to build them?",
      },
    ],
    interviewChallenge: [
      "What used to cause OutOfMemoryError: PermGen space before Java 8, and why is that less common now?",
      "Can Metaspace still run out of memory? Under what circumstances?",
      "Why does loading thousands of dynamically generated classes risk filling Metaspace?",
    ],
  },
  {
    id: 'memory-leak',
    order: 43,
    tier: 'advanced',
    title: 'Memory Leak',
    tagline: "A dripping faucet into a bucket nobody empties",
    expandReaction: "This is a real production bug you'll actually debug someday — worth understanding precisely, not just the phrase.",
    analogy: {
      emoji: '🚰',
      text: "A memory leak is like a dripping faucet you never notice — each drop alone is nothing, but if it keeps dripping into a bucket nobody ever empties, eventually it overflows. In Java, a memory leak isn't memory the garbage collector forgot about — it's memory that's technically still reachable (so GC can't touch it), but the program will never actually use again.",
    },
    explainer: "Since Java has automatic garbage collection, \"true\" memory leaks like in C are rare — but a Java memory leak still happens when objects are unintentionally kept reachable, for example added to a static collection and never removed, or held by a listener that's never unregistered. The GC can never reclaim them, so memory usage keeps growing, potentially until OutOfMemoryError.",
    codeExample: {
      label: 'In real code',
      code: `static List<byte[]> cache = new ArrayList<>(); // static = lives for the whole program\n\nvoid processRequest() {\n  cache.add(new byte[1_000_000]); // 1MB added every call, never removed\n}\n// after many calls, cache keeps growing forever — a classic Java memory leak`,
      output: [
        'Each processRequest() call adds 1MB to a static list that\'s never cleared',
        'The objects ARE still reachable (through the static cache reference) — GC can\'t touch them',
        'cache keeps growing → eventually memory runs out',
        '✓ Not "GC forgot" — the code itself kept unwanted references alive',
      ],
    },
    kaiClaim: {
      text: "So a memory leak in Java means the garbage collector has a bug and misses some unreachable objects?",
      isCorrect: false,
      whyRight: "No — the GC is working exactly as designed. A Java memory leak happens when the program itself accidentally keeps objects reachable, like in a growing static list, that it doesn't actually need anymore.",
    },
    quiz: [
      {
        id: 'memory-leak-q1',
        question: 'Why can\'t the garbage collector reclaim the byte arrays being added to `cache` above?',
        options: ['They\'re still reachable through the static cache reference, so GC considers them "in use"', 'The garbage collector is broken in this JVM version', 'byte[] objects are immune to garbage collection'],
        correctIndex: 0,
        explainCorrect: "Right — reachability, not a GC bug, is exactly what's keeping this memory alive.",
        explainIncorrect: "Not quite — there's no JVM bug here, and byte[] objects are collected like anything else once unreachable.",
        hint: "Think about the dripping faucet again — is the bucket ever emptied?",
      },
    ],
    interviewChallenge: [
      "Why are memory leaks in Java usually 'logical' rather than the GC actually failing?",
      "Name two common real-world causes of memory leaks in long-running Java applications.",
      "How would you use a heap dump to track down a suspected memory leak?",
    ],
  },
  {
    id: 'outofmemoryerror',
    order: 44,
    tier: 'advanced',
    title: 'OutOfMemoryError',
    tagline: "A warehouse that's genuinely, completely full",
    expandReaction: "Last one — this is what all that heap, GC, and memory-leak knowledge actually gets used to debug.",
    analogy: {
      emoji: '💥',
      text: "OutOfMemoryError is like a warehouse that's completely full — no matter how hard the cleaning crew (the garbage collector) tries, there's genuinely no more space, and no amount of tidying frees up room for the new shipment that just arrived.",
    },
    explainer: "OutOfMemoryError is thrown when the JVM cannot allocate more memory in a given area (heap, Metaspace, and so on) even after running garbage collection to try to free space. Common causes include a genuine memory leak, too many large objects for the configured heap size, or a heap that's simply too small for the workload.",
    codeExample: {
      label: 'In real code',
      code: `List<int[]> hog = new ArrayList<>();\nwhile (true) {\n  hog.add(new int[1_000_000]); // keeps allocating, nothing ever freed\n}\n// eventually: Exception in thread "main" java.lang.OutOfMemoryError: Java heap space`,
      output: [
        'Loop keeps allocating large arrays, held by hog, never released',
        'Heap keeps filling up',
        'Eventually: OutOfMemoryError: Java heap space',
        '✓ GC tried its best, but there was truly nothing left to reclaim',
      ],
    },
    kaiClaim: {
      text: "So OutOfMemoryError means there's a bug in the JVM itself?",
      isCorrect: false,
      whyRight: "No — it almost always means the program's own code is either genuinely leaking memory or just needs more heap than it's been given. The JVM is reporting a real resource limit, not failing on its own.",
    },
    quiz: [
      {
        id: 'outofmemoryerror-q1',
        question: 'What does OutOfMemoryError actually indicate?',
        options: ["The JVM couldn't free enough memory even after running garbage collection", 'A syntax error in the Java source code', 'The CPU is running too many threads at once'],
        correctIndex: 0,
        explainCorrect: "Right — it's a genuine resource limit, hit even after the GC tried to reclaim space.",
        explainIncorrect: "Not quite — this isn't a syntax error (that would fail at compile time), and it's not directly about thread count.",
        hint: "Think about the full warehouse again — is this a cleaning-crew problem, or a genuinely-nothing-left problem?",
      },
    ],
    interviewChallenge: [
      "What's the difference between OutOfMemoryError: Java heap space and OutOfMemoryError: Metaspace?",
      "Is OutOfMemoryError a checked exception, an unchecked exception, or something else entirely?",
      "What's the very first thing you'd check in production if you saw this error?",
    ],
  },
  {
    id: 'variables',
    order: 45,
    tier: 'beginner',
    title: 'Variables',
    tagline: 'A labeled box holding one value at a time',
    expandReaction: "Foundational, but worth being precise about — this is where Java's static typing first shows up.",
    analogy: {
      emoji: '🏷️',
      text: "A variable is like a labeled box on a shelf — the label (name) stays the same, but you can open the box and swap what's inside, as long as the new thing fits the box's declared shape.",
    },
    explainer: "In Java, a variable is a named storage location with a declared type. Unlike Python, Java is statically typed — once you declare `int age`, that box can only ever hold an int; the type is locked in at declaration and checked by the compiler before the code even runs.",
    codeExample: {
      label: 'In real code',
      code: `int age = 23;      // a box labeled "age", declared to hold only ints\nage = 24;          // same box, new value, same type\n// age = "twenty-four"; // ✗ won't compile — wrong "shape" for this box`,
      output: [
        'age = 23 → box created, holds an int',
        'age = 24 → same box, value swapped',
        'age = "twenty-four" → 💥 compile error, wrong type for this box',
        '✓ The label (variable name) stays fixed; only the value inside can change, and only to a matching type',
      ],
    },
    kaiClaim: {
      text: "So in Java, could I put a String into an int variable if I really needed to?",
      isCorrect: false,
      whyRight: "No — Java checks types at compile time. An int variable can only ever hold int values (or things that convert automatically); a String just won't fit, and the compiler stops you before the program even runs.",
    },
    quiz: [
      {
        id: 'variables-q1',
        question: 'Why does `age = "twenty-four";` fail to compile if age was declared as `int age`?',
        options: ["Java is statically typed — a variable's declared type is locked in and checked at compile time", 'Java allows any value in any variable, but only at runtime', 'The equals sign needs to be doubled for strings'],
        correctIndex: 0,
        explainCorrect: "Right — static typing means the compiler catches the mismatch before the program ever runs.",
        explainIncorrect: "Not quite — this isn't a runtime-only check, and it has nothing to do with the equals sign.",
        hint: "Think about the labeled box again — what happens if you try to stuff the wrong-shaped item into it?",
      },
    ],
    interviewChallenge: [
      "Why does Java require you to declare a variable's type before using it?",
      'What happens if you try to use a local variable before assigning it an initial value?',
    ],
  },
  {
    id: 'data-types',
    order: 46,
    tier: 'beginner',
    title: 'Data Types',
    tagline: 'Raw ingredients vs a finished cake on a stand',
    expandReaction: "This distinction explains a lot of confusing Java behavior later — worth getting solid early.",
    analogy: {
      emoji: '🧱',
      text: "Primitive types are like raw ingredients — flour, sugar, eggs — simple and stored directly. Reference types are like a finished cake on a stand, where what you're actually holding is a card telling you where to find the cake, not the cake itself.",
    },
    explainer: "Java has 8 primitive types (int, double, boolean, char, etc.) that store actual values directly. Everything else — String, arrays, your own classes — is a reference type: the variable holds a reference pointing to an object on the heap, not the data itself.",
    codeExample: {
      label: 'In real code',
      code: `int x = 5;           // primitive — the actual value 5 is stored directly\nInteger boxed = 5;   // reference type — boxed points to an Integer object holding 5\n\nint[] arr = {1, 2, 3}; // arr is a reference to an array object on the heap`,
      output: [
        'x → stores the value 5 directly',
        'boxed → a reference pointing to an Integer object elsewhere',
        'arr → a reference pointing to an array object elsewhere',
        '✓ Primitives hold the value itself; reference types hold directions to where the value actually lives',
      ],
    },
    kaiClaim: {
      text: "So Integer and int are basically identical, just different spellings?",
      isCorrect: false,
      whyRight: "No — int is a primitive holding a raw value directly, while Integer is a reference type that wraps an int, adds overhead, and can even be null, which int can never be.",
    },
    quiz: [
      {
        id: 'data-types-q1',
        question: "What's the key difference between an int and an Integer in Java?",
        options: ['int stores a raw value directly; Integer is an object reference that wraps an int and can be null', 'They are exactly the same thing with two names', 'Integer is faster than int in every situation'],
        correctIndex: 0,
        explainCorrect: "Right — one holds a raw value, the other is a full object reference.",
        explainIncorrect: "Not quite — they aren't identical, and Integer typically has more overhead, not less.",
        hint: "Think about raw ingredients vs. a cake on a stand — one holds the thing itself, the other holds a pointer to it.",
      },
    ],
    interviewChallenge: [
      'Why can an Integer be null but an int can never be?',
      "What's autoboxing, and where can it silently hurt performance?",
    ],
  },
  {
    id: 'strings',
    order: 47,
    tier: 'beginner',
    title: 'Strings',
    tagline: 'Once written, never rewritten',
    expandReaction: "String immutability quietly explains a lot of Java's design — worth understanding deeply, not just memorizing.",
    analogy: {
      emoji: '🧵',
      text: "A Java String is like a name carved in stone — once carved, that exact stone can never be re-carved. If you 'change' it, you're not editing the stone, you're carving a brand new one and pointing your sign to it instead.",
    },
    explainer: "Strings in Java are immutable — once created, a String's contents can never change. Operations like concat() or `+` don't modify the original String; they create and return a brand new one. This immutability is also what makes the String pool possible: literal Strings with the same content can safely share the same object.",
    codeExample: {
      label: 'In real code',
      code: `String a = "hello";\nString b = a.concat(" world");\nSystem.out.println(a); // "hello" — unchanged!\nSystem.out.println(b); // "hello world" — a brand new String\n\nString x = "cat";\nString y = "cat";\nSystem.out.println(x == y); // true — both point to the same pooled literal`,
      output: [
        'a → still "hello", untouched',
        'b → a completely new String, "hello world"',
        'x == y → true (same object in the String pool)',
        '✓ Nothing about the original "hello" ever changed — concat() built something new',
      ],
    },
    kaiClaim: {
      text: 'So a.concat(" world") actually modifies the original String a in place?',
      isCorrect: false,
      whyRight: "No — Strings are immutable in Java. concat() returns a brand new String; the original a is completely untouched.",
    },
    quiz: [
      {
        id: 'strings-q1',
        question: 'After `String b = a.concat(" world");`, what does `a` actually contain?',
        options: ['Still exactly what it was before — "hello", unchanged', '"hello world", same as b', 'null, because concat() consumes the original'],
        correctIndex: 0,
        explainCorrect: "Right — a is completely untouched; concat() only produced a new String.",
        explainIncorrect: "Not quite — a isn't modified or nulled out, it stays exactly as it was.",
        hint: "Think about the stone carving again — does carving a new stone erase the old one?",
      },
    ],
    interviewChallenge: [
      "Why can Strings be safely used as HashMap keys, but a mutable object often can't?",
      "What's the difference between == and .equals() when comparing two Strings?",
    ],
  },
  {
    id: 'arrays',
    order: 48,
    tier: 'beginner',
    title: 'Arrays',
    tagline: 'A row of numbered lockers, fixed at creation',
    expandReaction: "Last of the beginner batch — this sets up exactly why List/ArrayList exists at all.",
    analogy: {
      emoji: '🗄️',
      text: "An array is like a row of numbered lockers bolted to a wall — you decide how many lockers exist the moment they're installed, and that number can never change afterward. You can put a new item in any locker anytime, but you can't add a 51st locker to a 50-locker row.",
    },
    explainer: "A Java array has a fixed size decided at creation time — you can reassign the value at any index, but you can never grow or shrink the array itself. If you need a resizable container, you'd reach for a List (like ArrayList) instead, which is built on top of arrays but handles the resizing for you.",
    codeExample: {
      label: 'In real code',
      code: `int[] scores = new int[3]; // 3 lockers, fixed forever\nscores[0] = 90;\nscores[1] = 85;\nscores[2] = 78;\n// scores[3] = 100; // 💥 ArrayIndexOutOfBoundsException — no 4th locker exists\nSystem.out.println(scores.length); // 3, always`,
      output: [
        'scores → a fixed row of exactly 3 lockers',
        'scores[3] = 100 → 💥 ArrayIndexOutOfBoundsException, no such locker',
        'scores.length → 3, forever',
        '✓ Values inside the lockers can change; the number of lockers never can',
      ],
    },
    kaiClaim: {
      text: "So I could just add a 4th score to that array later if I need more room?",
      isCorrect: false,
      whyRight: "No — a Java array's size is fixed the moment it's created. To hold a 4th score, you'd need a brand new, bigger array (or a resizable List instead).",
    },
    quiz: [
      {
        id: 'arrays-q1',
        question: 'Why does `scores[3] = 100;` throw an exception if scores was created with `new int[3]`?',
        options: ["The array only has 3 slots (indices 0-2); index 3 doesn't exist", 'Arrays can only ever hold exactly 3 elements in Java', 'The value 100 is too large to store'],
        correctIndex: 0,
        explainCorrect: "Right — index 3 is simply out of bounds for a 3-element array.",
        explainIncorrect: "Not quite — it's not that 3 is a magic max, and 100 isn't too large a value; it's purely an out-of-bounds index.",
        hint: "Think about the row of lockers again — if there are 3 lockers numbered 0, 1, 2, is there a locker numbered 3?",
      },
    ],
    interviewChallenge: [
      "What's the actual difference between an array and an ArrayList in terms of resizing?",
      'Why is System.arraycopy() often faster than copying an array with a manual loop?',
    ],
  },
  {
    id: 'exceptions',
    order: 49,
    tier: 'intermediate',
    title: 'Exceptions (Checked vs Unchecked)',
    tagline: 'A warning sign you must plan for vs a sudden pothole',
    expandReaction: "The checked/unchecked split is one of the most commonly misunderstood parts of Java's exception model.",
    analogy: {
      emoji: '🚦',
      text: "A checked exception is like a road sign warning 'bridge ahead may be closed' — the compiler forces you to have a plan before you're even allowed to drive that road. An unchecked exception is like a sudden pothole — nothing warned you in advance, and the compiler doesn't require you to prepare for it.",
    },
    explainer: "Checked exceptions (like IOException) extend Exception and must be either caught or declared with `throws` — the compiler enforces this. Unchecked exceptions (like NullPointerException) extend RuntimeException and require no such declaration. Many teams intentionally convert checked exceptions to unchecked ones to avoid forcing handling everywhere up the call stack.",
    codeExample: {
      label: 'In real code',
      code: `// Checked — must be caught or declared\nvoid readFile() throws IOException {\n  new FileReader("data.txt"); // compiler forces you to handle this possibility\n}\n\n// Unchecked — no such requirement\nvoid divide(int a, int b) {\n  System.out.println(a / b); // ArithmeticException possible, compiler won't force handling\n}`,
      output: [
        'readFile() → compiler requires throws IOException or a try/catch around the risky call',
        'divide() → compiles fine even though a / 0 could throw at runtime',
        '✓ Checked = compiler-enforced planning ahead. Unchecked = no such enforcement.',
      ],
    },
    kaiClaim: {
      text: "So checked exceptions are just the 'more serious' ones, and unchecked ones are minor?",
      isCorrect: false,
      whyRight: "No — severity isn't the distinction, it's purely compiler enforcement. A NullPointerException (unchecked) can absolutely be a serious bug.",
    },
    quiz: [
      {
        id: 'exceptions-q1',
        question: 'What does the Java compiler force you to do differently for a checked exception compared to an unchecked one?',
        options: ["Either catch it or declare it with `throws`, or the code won't compile", 'Nothing — both are treated identically by the compiler', 'Checked exceptions must always print a stack trace'],
        correctIndex: 0,
        explainCorrect: "Right — that compile-time enforcement is the entire distinction.",
        explainIncorrect: "Not quite — the compiler treats them very differently, and neither type is required to print a stack trace.",
        hint: "Think about the road sign again — does it just warn, or does it actually block you from driving until you have a plan?",
      },
    ],
    interviewChallenge: [
      'Why do many teams avoid checked exceptions for business logic errors?',
      "What happens to an exception if it's never caught anywhere up the call stack?",
    ],
  },
  {
    id: 'generics',
    order: 50,
    tier: 'intermediate',
    title: 'Generics',
    tagline: 'A labeled ice tray instead of a mystery bag',
    expandReaction: "This is what makes collections type-safe — worth connecting back to the ClassCastException it prevents.",
    analogy: {
      emoji: '🧊',
      text: "A raw, un-generic collection is like a mystery grab-bag — you can put anything in and pull anything out, but you won't know what you actually got until you check. Generics are like an ice tray labeled 'water only' — the compiler enforces that only the right type ever goes in.",
    },
    explainer: "Generics let you parameterize a class or method with a type (like List<String>), so the compiler checks type-correctness at compile time instead of you discovering a ClassCastException at runtime. Before generics, code often stored raw Objects and cast them back, which was unsafe.",
    codeExample: {
      label: 'In real code',
      code: `List<Object> mystery = new ArrayList<>();\nmystery.add("hello");\nmystery.add(42); // no compiler complaint — mixed types allowed\nString s = (String) mystery.get(1); // 💥 ClassCastException at runtime\n\nList<String> safe = new ArrayList<>();\nsafe.add("hello");\n// safe.add(42); // ✗ won't even compile — caught immediately`,
      output: [
        'mystery.get(1) cast to String → 💥 ClassCastException, discovered only at runtime',
        'safe.add(42) → ✗ compile error, caught immediately, before the program ever runs',
        '✓ Generics move type mistakes from a runtime crash to a compile-time error',
      ],
    },
    kaiClaim: {
      text: "So generics just make the code shorter to write, with no real safety benefit?",
      isCorrect: false,
      whyRight: "No — the real benefit is catching type mistakes at compile time instead of at runtime. Shorter code is a side effect, not the main point.",
    },
    quiz: [
      {
        id: 'generics-q1',
        question: 'Why does `safe.add(42);` fail to even compile, while `mystery.add(42);` compiles fine?',
        options: ['safe is declared List<String>, so generics enforce only Strings are allowed in', 'mystery.add() is a completely different method', '42 is not a valid Java value'],
        correctIndex: 0,
        explainCorrect: "Right — the generic type parameter is what enforces this at compile time.",
        explainIncorrect: "Not quite — it's the same add() method; 42 is a perfectly valid Java value, just the wrong type for this generic list.",
        hint: 'Think about the labeled ice tray again — what happens if you try to pour juice into a tray labeled "water only"?',
      },
    ],
    interviewChallenge: [
      'What is type erasure, and why can\'t you check `list instanceof List<String>` at runtime?',
      "Why can't you create a generic array directly, like `new T[10]`?",
    ],
  },
  {
    id: 'streams',
    order: 51,
    tier: 'intermediate',
    title: 'Streams',
    tagline: 'A conveyor belt of transformations, not a container',
    expandReaction: "Streams replace a lot of manual loop-and-accumulate code — worth seeing the laziness clearly.",
    analogy: {
      emoji: '🌊',
      text: "A Java Stream is like a conveyor belt at a factory — items move along, get filtered, get reshaped, get combined into a final product, but the belt itself never actually stores the items. Once something rolls off the end, the belt is empty and ready for a new run.",
    },
    explainer: "The Stream API lets you express a pipeline of operations (filter, map, sorted, collect, etc.) over a data source declaratively, rather than writing manual loops. Streams are lazy — nothing runs until a terminal operation like collect() is called — and can only be consumed once; reusing an already-consumed stream throws an IllegalStateException.",
    codeExample: {
      label: 'In real code',
      code: `List<String> names = List.of("Amit", "Zara", "Riya", "Amit");\nList<String> result = names.stream()\n    .filter(n -> !n.equals("Amit"))\n    .map(String::toUpperCase)\n    .sorted()\n    .collect(Collectors.toList());\nSystem.out.println(result);`,
      output: [
        'names.stream() → starts the pipeline (nothing runs yet — lazy)',
        '.filter(...).map(...).sorted() → describes the transformations',
        '.collect(...) → the terminal operation that actually triggers everything',
        'result → [RIYA, ZARA]',
      ],
    },
    kaiClaim: {
      text: "So calling `names.stream().filter(...)` immediately filters the list right there?",
      isCorrect: false,
      whyRight: "No — streams are lazy. Nothing actually runs until a terminal operation like collect() or forEach() is called.",
    },
    quiz: [
      {
        id: 'streams-q1',
        question: 'What actually triggers the filter/map/sorted pipeline above to run?',
        options: ['The terminal operation, collect(), at the end', 'Calling names.stream() at the very start', 'Declaring the result variable'],
        correctIndex: 0,
        explainCorrect: "Right — nothing executes until the terminal operation is reached.",
        explainIncorrect: "Not quite — stream() just opens the pipeline, and declaring a variable triggers nothing on its own.",
        hint: 'Think about the conveyor belt again — does it start moving the instant you look at it, or only once something at the end actually asks for output?',
      },
    ],
    interviewChallenge: [
      'Why can a Stream only be consumed once?',
      "What's the difference between an intermediate operation (like filter) and a terminal operation (like collect)?",
    ],
  },
  {
    id: 'lambdas',
    order: 52,
    tier: 'intermediate',
    title: 'Lambdas',
    tagline: 'A function, written as a value, with no name needed',
    expandReaction: "Pairs directly with Streams — most stream pipelines are built almost entirely out of small lambdas.",
    analogy: {
      emoji: '🎯',
      text: "A lambda is like ordering food with a quick hand gesture instead of writing a full request form — 'this one, please' — you skip naming and formally defining a whole new method just to describe one small, one-off action.",
    },
    explainer: "A lambda expression is a compact way to implement a functional interface (an interface with exactly one abstract method) inline, without writing a separate named class. Lambdas are used heavily with the Stream API and anywhere a small piece of behavior needs to be passed around as a value.",
    codeExample: {
      label: 'In real code',
      code: `// Without a lambda: a full anonymous class just to compare two strings by length\nComparator<String> byLength = new Comparator<String>() {\n  public int compare(String a, String b) { return a.length() - b.length(); }\n};\n\n// With a lambda: the same behavior, far less ceremony\nComparator<String> byLengthLambda = (a, b) -> a.length() - b.length();`,
      output: [
        'byLength and byLengthLambda → do exactly the same comparison',
        '✓ The lambda is just a shorter way to write the same single-method implementation',
      ],
    },
    kaiClaim: {
      text: "So a lambda can implement any interface, no matter how many methods it has?",
      isCorrect: false,
      whyRight: "No — a lambda can only implement a functional interface, one with exactly one abstract method.",
    },
    quiz: [
      {
        id: 'lambdas-q1',
        question: 'What kind of interface can a lambda expression actually implement?',
        options: ['A functional interface — one with exactly one abstract method', 'Any interface, regardless of how many methods it declares', 'Only interfaces with zero methods'],
        correctIndex: 0,
        explainCorrect: "Right — exactly one abstract method is the requirement.",
        explainIncorrect: "Not quite — a lambda can't stand in for multiple abstract methods, and it can't implement an interface with zero abstract methods either.",
        hint: 'Think about the quick hand gesture again — can one gesture really replace an entire multi-step form?',
      },
    ],
    interviewChallenge: [
      "Can a lambda access a local variable from its enclosing scope? What's the restriction?",
      "What's the functional interface behind a lambda like `(a, b) -> a + b`?",
    ],
  },
  {
    id: 'file-handling',
    order: 53,
    tier: 'intermediate',
    title: 'File Handling',
    tagline: 'Borrow a resource, and always give it back',
    expandReaction: "Last of the intermediate batch — try-with-resources is the modern default, worth knowing exactly why.",
    analogy: {
      emoji: '📁',
      text: "Opening a file is like borrowing a library book — you're expected to return it when done, even if you got distracted (an error) partway through. try-with-resources is like an automatic return slot that takes the book back for you the moment you're finished, no matter what happened while you had it.",
    },
    explainer: "Reading or writing a file opens a resource (like a FileReader) that must be closed afterward to release the underlying file handle. try-with-resources (any resource implementing AutoCloseable) automatically calls close() when the try block ends — including if an exception was thrown — so you never need a manual finally block just to clean up.",
    codeExample: {
      label: 'In real code',
      code: `try (BufferedReader reader = new BufferedReader(new FileReader("data.txt"))) {\n  String line = reader.readLine();\n  System.out.println(line);\n} // reader.close() is called automatically here, even if readLine() threw\ncatch (IOException e) {\n  System.out.println("Could not read file: " + e.getMessage());\n}`,
      output: [
        'reader opens "data.txt"',
        'reader.readLine() → reads the first line',
        'try block ends → reader.close() runs automatically',
        '✓ The file handle is released whether the read succeeded or an exception was thrown',
      ],
    },
    kaiClaim: {
      text: "So if readLine() throws an exception, the file just stays open forever since we never reached a manual close()?",
      isCorrect: false,
      whyRight: "No — that's exactly the problem try-with-resources solves. It calls close() automatically as the try block exits, even if an exception was thrown partway through.",
    },
    quiz: [
      {
        id: 'file-handling-q1',
        question: 'Why is try-with-resources safer than manually calling reader.close() at the end of the try block?',
        options: ["It closes the resource automatically even if an exception is thrown before a manual close() would run", 'It reads files faster than a normal FileReader', 'It automatically fixes any IOException that occurs'],
        correctIndex: 0,
        explainCorrect: "Right — automatic cleanup on any exit path is the whole benefit.",
        explainIncorrect: "Not quite — it doesn't change read speed, and it doesn't fix exceptions, just guarantees cleanup.",
        hint: 'Think about the automatic library return slot again — does it care whether you finished the book, or got interrupted partway through?',
      },
    ],
    interviewChallenge: [
      "What happens if both the try block and the resource's close() throw exceptions in try-with-resources?",
      'Why should file streams always be closed, even on the success path?',
    ],
  },
  {
    id: 'multithreading',
    order: 54,
    tier: 'advanced',
    title: 'Multithreading',
    tagline: 'Multiple cooks in one kitchen, sharing the same counter',
    expandReaction: "This is where race conditions come from — worth seeing exactly why `count++` isn't as safe as it looks.",
    analogy: {
      emoji: '🧑‍🍳',
      text: "Multithreading is like multiple cooks working in the same kitchen at once — great for getting more done, but if two cooks reach for the same bowl of batter without coordinating, you get a race condition: whoever grabs it last overwrites what the other one just did.",
    },
    explainer: "A Java thread is an independent path of execution that can run concurrently with others. Multiple threads sharing data (like an instance field) can produce a race condition if they read-modify-write it without synchronization — the `synchronized` keyword (or higher-level locks) ensures only one thread touches the shared data at a time.",
    codeExample: {
      label: 'In real code',
      code: `class Counter {\n  private int count = 0;\n  synchronized void increment() { count++; } // only one thread at a time can run this\n  int getCount() { return count; }\n}\n\n// Without synchronized, two threads calling increment() simultaneously\n// could both read count=5, both compute 6, and only one increment is recorded`,
      output: [
        'Without synchronized → two threads racing on count++ can lose an update (race condition)',
        'With synchronized → only one thread executes increment() at a time — no lost updates',
        '✓ synchronized turns a risky shared read-modify-write into a safe, one-at-a-time operation',
      ],
    },
    kaiClaim: {
      text: "So count++ is a single, safe operation — it can't be interrupted by another thread?",
      isCorrect: false,
      whyRight: "No — count++ is actually three separate steps (read, increment, write). Without synchronization, two threads can interleave those steps and lose an update.",
    },
    quiz: [
      {
        id: 'multithreading-q1',
        question: "Why can two threads calling increment() at the same time (without `synchronized`) end up losing an update?",
        options: ["count++ is actually read-modify-write; two threads can interleave those steps and overwrite each other's result", 'Java threads always run in a fixed, safe order automatically', 'Integers in Java cannot be modified by more than one thread ever'],
        correctIndex: 0,
        explainCorrect: "Right — the interleaving of read-modify-write steps is exactly the race condition.",
        explainIncorrect: "Not quite — thread execution order isn't automatically safe, and Integers can absolutely be touched by multiple threads.",
        hint: 'Think about the two cooks reaching for the same bowl — what happens if neither one waits for the other?',
      },
    ],
    interviewChallenge: [
      "What's the difference between `synchronized` and `volatile`, and when is volatile actually enough?",
      'Why does creating a new Thread for every incoming request in a loop scale badly?',
    ],
  },
  {
    id: 'concurrent-collections',
    order: 55,
    tier: 'advanced',
    title: 'Concurrent Collections',
    tagline: 'A shared basket built to survive multiple hands at once',
    expandReaction: "Builds directly on ConcurrentHashMap — this generalizes to the rest of java.util.concurrent.",
    analogy: {
      emoji: '🧺',
      text: "A regular collection is like a basket meant for one person at a time — if two people rearrange it simultaneously, things get corrupted. Concurrent collections are like a basket specifically engineered so multiple people can safely reach in and take or add items at the same time.",
    },
    explainer: "The java.util.concurrent package provides collections designed for safe multi-threaded access — like ConcurrentHashMap, CopyOnWriteArrayList, and BlockingQueue — without manually wrapping them in synchronized blocks. These typically offer better throughput than externally synchronizing a plain collection, since they use finer-grained internal coordination instead of one big lock for every operation.",
    codeExample: {
      label: 'In real code',
      code: `// A thread-safe cache with O(1) get/put, safe under concurrent access:\nMap<String, String> cache = new ConcurrentHashMap<>();\ncache.put("u1", "Amit");\n\n// Multiple threads can safely read/write different keys at the same time,\n// without needing an external synchronized block around every access\nString u = cache.get("u1");`,
      output: [
        'cache.put(...) / cache.get(...) → safe even if called from many threads simultaneously',
        'No external synchronized block needed around each access',
        '✓ The collection itself handles safe concurrent access internally',
      ],
    },
    kaiClaim: {
      text: "So any regular HashMap becomes just as safe as ConcurrentHashMap if you just call it from multiple threads carefully?",
      isCorrect: false,
      whyRight: "No — a plain HashMap isn't designed for concurrent structural changes at all; it can even corrupt its internal structure under concurrent modification. You'd need external synchronization or a purpose-built concurrent collection.",
    },
    quiz: [
      {
        id: 'concurrent-collections-q1',
        question: 'What\'s the main advantage of ConcurrentHashMap over manually wrapping a HashMap in a synchronized block?',
        options: ['It allows much better throughput by avoiding one single lock for every operation', 'It uses less memory than a regular HashMap', "It automatically sorts keys as they're inserted"],
        correctIndex: 0,
        explainCorrect: "Right — finer-grained locking is exactly what gives it better concurrent throughput.",
        explainIncorrect: "Not quite — memory use isn't the main difference, and it doesn't sort anything.",
        hint: 'Think about the basket built for multiple hands again — is it just "locked" the same way, or built differently altogether?',
      },
    ],
    interviewChallenge: [
      'Why might CopyOnWriteArrayList be a bad choice for a list with frequent writes?',
      'What trade-off does ConcurrentHashMap make to avoid locking the whole map?',
    ],
  },
  {
    id: 'design-patterns',
    order: 56,
    tier: 'advanced',
    title: 'Design Patterns',
    tagline: 'A recipe other cooks already perfected',
    expandReaction: "Singleton is the most commonly interview-tested pattern — worth knowing its pitfalls, not just its definition.",
    analogy: {
      emoji: '📐',
      text: "A design pattern is like a well-known recipe that generations of cooks have already refined — you don't have to invent 'how to make a good omelet' from scratch every time. The Singleton pattern is the recipe for 'make sure there's only ever one of this in the whole kitchen.'",
    },
    explainer: "Design patterns are reusable, named solutions to common software design problems, giving developers a shared vocabulary. The Singleton pattern ensures a class has exactly one instance across the whole application, with one global access point to it — commonly used for things like a shared configuration object or connection pool.",
    codeExample: {
      label: 'In real code',
      code: `class ConfigManager {\n  private static final ConfigManager INSTANCE = new ConfigManager();\n  private ConfigManager() {} // private constructor — nobody else can call \`new\`\n  static ConfigManager getInstance() { return INSTANCE; }\n}\n\nConfigManager a = ConfigManager.getInstance();\nConfigManager b = ConfigManager.getInstance();\nSystem.out.println(a == b); // true — always the exact same instance`,
      output: [
        'ConfigManager.getInstance() called twice',
        'a == b → true — both references point to the exact same single object',
        '✓ No matter how many times getInstance() is called, only one ConfigManager ever exists',
      ],
    },
    kaiClaim: {
      text: "So I could still create a second ConfigManager myself with `new ConfigManager()` from another class?",
      isCorrect: false,
      whyRight: "No — the constructor is private, so no class outside ConfigManager itself can call `new ConfigManager()`. That's exactly what enforces there only ever being one instance.",
    },
    quiz: [
      {
        id: 'design-patterns-q1',
        question: 'What actually prevents another class from creating a second ConfigManager instance?',
        options: ['The constructor is private, so `new ConfigManager()` cannot be called from outside the class', 'Java automatically limits every class to one instance', 'getInstance() deletes any earlier instance before creating a new one'],
        correctIndex: 0,
        explainCorrect: "Right — the private constructor is what actually enforces the singleton property.",
        explainIncorrect: "Not quite — Java has no such automatic limit, and getInstance() never deletes anything, it just returns the same instance.",
        hint: "Think about the recipe again — what's actually stopping someone else in the kitchen from just making their own version from scratch?",
      },
    ],
    interviewChallenge: [
      'What are the pitfalls of double-checked locking in a thread-safe Singleton?',
      'When would you reach for the Builder pattern instead of a constructor with many parameters?',
    ],
  },
  {
    id: 'performance',
    order: 57,
    tier: 'advanced',
    title: 'Performance',
    tagline: 'Knowing which shelf your code is reaching into',
    expandReaction: "Last of the advanced batch — ties directly back to ArrayList vs LinkedList and HashMap vs List earlier.",
    analogy: {
      emoji: '📊',
      text: "Writing fast code isn't about typing quickly — it's like knowing whether you're reaching into a nearby drawer (fast) or walking to a warehouse across town (slow) every time you need something. The same task can be instant or painfully slow purely based on which data structure or algorithm you reached for.",
    },
    explainer: "Performance in Java often comes down to choosing the right data structure for the access pattern you need — an ArrayList.get(i) is O(1), but a LinkedList.get(i) is O(n); a HashMap lookup is O(1) average, but a linear search through a List is O(n). Understanding these Big-O trade-offs lets you predict how code will behave at scale, before it ever becomes a production problem.",
    codeExample: {
      label: 'In real code',
      code: `List<Integer> bigList = new ArrayList<>(); // 1,000,000 elements\n// contains() on a List is O(n) — checks every element one by one\nboolean found = bigList.contains(999999); // slow at scale\n\nSet<Integer> bigSet = new HashSet<>(bigList);\n// contains() on a HashSet is O(1) average — direct hash lookup\nboolean foundFast = bigSet.contains(999999); // fast at scale`,
      output: [
        'bigList.contains(...) → O(n), checks elements one by one',
        'bigSet.contains(...) → O(1) average, direct hash-based lookup',
        '✓ Same logical check, wildly different cost — purely because of the container chosen',
      ],
    },
    kaiClaim: {
      text: "So contains() always has the exact same cost, no matter which collection you call it on?",
      isCorrect: false,
      whyRight: "No — contains() on a List is O(n), while contains() on a HashSet is O(1) average. The collection you choose directly determines the cost.",
    },
    quiz: [
      {
        id: 'performance-q1',
        question: 'Why is bigSet.contains(999999) so much faster than bigList.contains(999999) at scale?',
        options: ['HashSet uses hashing for O(1) average lookup; List has to scan elements one by one, O(n)', 'HashSet and List actually have identical performance', 'bigList secretly converts to a Set internally'],
        correctIndex: 0,
        explainCorrect: "Right — the underlying data structure is what determines the cost here.",
        explainIncorrect: "Not quite — the performance is genuinely different, and no silent conversion happens.",
        hint: 'Think about the nearby drawer vs. the warehouse across town — which one do you want to check a thousand times?',
      },
    ],
    interviewChallenge: [
      'Why can premature optimization sometimes make code harder to maintain for no real benefit?',
      'How would you actually measure whether one implementation is faster than another, rather than guessing?',
    ],
  },
  {
    id: 'frequently-asked-why',
    order: 58,
    tier: 'interview-mastery',
    title: 'Frequently Asked "Why" Questions',
    tagline: "Knowing the fact isn't the same as knowing the why",
    expandReaction: "This is the single most common interview pattern — a correct fact followed immediately by 'why?'",
    analogy: {
      emoji: '🎤',
      text: "Anyone can memorize that 'the sky is blue' — an interviewer wants to know if you understand WHY (light scattering), not just that it is. Interviewers constantly follow a correct answer with 'why' to test whether you actually understand the mechanism, or just memorized the label.",
    },
    explainer: "A huge share of interview follow-ups are a single word: 'why?' Knowing that String is immutable isn't enough — you need to be ready to explain why: to enable safe sharing via the String pool, to make Strings safely usable as HashMap keys (a mutable key could change its hashCode after insertion, breaking the map), and to make Strings inherently thread-safe with no synchronization needed.",
    codeExample: {
      label: 'The pattern to prepare for',
      code: `// The fact: "String is immutable."\n// The "why" an interviewer actually wants:\n// 1. Safe to share via the String pool (nothing can mutate a shared instance)\n// 2. Safe as a HashMap key (hashCode never changes after insertion)\n// 3. Automatically thread-safe (no synchronization ever needed for reads)`,
      output: [
        'A memorized fact: "String is immutable" — a weak answer alone',
        'The reasoning behind it: pooling safety + stable hashCode + thread-safety — a strong answer',
        '✓ The "why" is what actually separates a memorized fact from real understanding',
      ],
    },
    kaiClaim: {
      text: "So as long as I state the correct fact confidently, that's basically as good as explaining why?",
      isCorrect: false,
      whyRight: "No — a confidently stated fact with no reasoning behind it is exactly what a 'why' follow-up is designed to expose. Interviewers are testing understanding, not recall.",
    },
    quiz: [
      {
        id: 'frequently-asked-why-q1',
        question: "If an interviewer asks 'why is HashMap's average lookup O(1)?' right after you say 'HashMap has O(1) lookup', what are they actually testing?",
        options: ['Whether you understand the underlying mechanism (hashing into buckets), not just the memorized fact', 'Whether you can repeat the same sentence a second time', 'Whether O(1) is definitely correct or not'],
        correctIndex: 0,
        explainCorrect: "Right — they're probing for real understanding of the mechanism, not repetition.",
        explainIncorrect: "Not quite — they're not just asking you to repeat yourself, and they're generally not doubting the O(1) fact itself.",
        hint: 'Think about the "sky is blue" example again — is the interviewer doubting the fact, or testing something else entirely?',
      },
    ],
    interviewChallenge: [
      'Pick any concept you just learned — could you explain WHY it works that way, not just WHAT it does?',
      "Why do interviewers ask 'why' instead of just checking if your answer matches a definition?",
    ],
  },
  {
    id: 'common-pitfalls',
    order: 59,
    tier: 'interview-mastery',
    title: 'Common Coding Pitfalls',
    tagline: '== compares addresses, .equals() compares contents',
    expandReaction: "One of the single most common real Java bugs — worth being airtight on this one.",
    analogy: {
      emoji: '⚠️',
      text: "== is like checking whether two people are standing in the exact same spot — .equals() is like checking whether they're wearing the same outfit. Two different people can wear identical outfits (equal content, different identity) — mixing these two checks up is one of the most common real Java bugs.",
    },
    explainer: "For objects (including String and boxed types like Integer), == compares references — whether both variables point to the literal same object in memory — while .equals() compares actual content. Because of String pooling and Integer caching (-128 to 127), == can accidentally 'work' for small test cases and then mysteriously fail in production with larger or non-pooled values.",
    codeExample: {
      label: 'In real code',
      code: `String a = new String("cat");\nString b = new String("cat");\nSystem.out.println(a == b);       // false — two different objects in memory\nSystem.out.println(a.equals(b));  // true — same actual content\n\nInteger x = 200, y = 200;\nSystem.out.println(x == y); // false — 200 is outside the cached range (-128 to 127)`,
      output: [
        'a == b → false (different objects, even with identical content)',
        'a.equals(b) → true (same content, which is what you almost always want)',
        'x == y (for 200) → false — surprises many people, since small Integers (-128..127) ARE cached',
        '✓ Always use .equals() for content comparison — == is for primitives or true identity checks only',
      ],
    },
    kaiClaim: {
      text: "So == and .equals() always give the same result for objects, since they're both just checking equality?",
      isCorrect: false,
      whyRight: "No — == checks whether two references point to the exact same object; .equals() checks whether their content is considered equal. For objects, these can very much disagree.",
    },
    quiz: [
      {
        id: 'common-pitfalls-q1',
        question: 'Why does `new String("cat") == new String("cat")` evaluate to false?',
        options: ['`new String(...)` always creates a distinct object, so == (reference comparison) sees two different objects', 'Java strings can never be compared with ==', 'Both Strings are actually null'],
        correctIndex: 0,
        explainCorrect: "Right — `new String(...)` deliberately creates a fresh object outside the pool.",
        explainIncorrect: "Not quite — == is legal on Strings (it just checks references), and neither String here is null.",
        hint: 'Think about two people wearing identical outfits again — are they standing in the same spot, just because they look the same?',
      },
    ],
    interviewChallenge: [
      'Why does comparing two boxed Integers above 127 with == sometimes silently break code that worked fine in testing?',
      'What\'s a real bug that == vs .equals() confusion could cause in production?',
    ],
  },
  {
    id: 'internal-implementation',
    order: 60,
    tier: 'interview-mastery',
    title: 'Internal Implementation: How HashMap Really Works',
    tagline: 'Peeking under the hood past the analogy',
    expandReaction: "This goes past the earlier HashMap analogy into the actual bucket/collision mechanics interviewers probe for.",
    analogy: {
      emoji: '🔬',
      text: "Earlier, HashMap was described as a library that shelves books by formula. Under the hood, that 'shelf' is actually an array of buckets — and when two books hash to the same shelf slot, they don't get lost, they just get hung on the same peg as a small chain (or, since Java 8, a mini sorted tree once that chain gets long enough).",
    },
    explainer: "Internally, HashMap holds an array of buckets. A key's hashCode() is run through an internal hash function to pick a bucket index. When two different keys land in the same bucket (a collision), Java 8+ stores them as a linked list within that bucket — and if a single bucket's chain grows past a threshold (8 entries), it's converted into a red-black tree for that bucket, turning worst-case lookup from O(n) down to O(log n).",
    codeExample: {
      label: 'A simplified mental model',
      code: `// buckets[ hash("Amit") % buckets.length ] -> [Entry("Amit", 23)]\n// buckets[ hash("Riya") % buckets.length ] -> [Entry("Riya", 25)]\n// If "Zara".hashCode() collides into the same bucket as "Amit":\n// buckets[ hash("Amit") % buckets.length ] -> [Entry("Amit", 23), Entry("Zara", 21)]\n// (chained together; treeified into a red-black tree if this chain gets long enough)`,
      output: [
        "Each key's hashCode() determines which bucket it lands in",
        'Collisions in the same bucket are chained (or treeified once the chain is long)',
        "✓ A poorly written hashCode() (e.g. always returning the same value) would silently wreck HashMap's performance — everything would collide into one bucket",
      ],
    },
    kaiClaim: {
      text: "So if two keys have the same hashCode(), HashMap just overwrites one entry with the other?",
      isCorrect: false,
      whyRight: "No — a hash collision doesn't overwrite anything. Both entries are kept, chained together (or treeified) within the same bucket, and HashMap uses equals() to tell them apart when looking one up.",
    },
    quiz: [
      {
        id: 'internal-implementation-q1',
        question: 'What actually happens when two different keys hash into the same bucket in a HashMap?',
        options: ['Both entries are kept, chained (or treeified) together in that bucket, distinguished later by equals()', 'The second key silently overwrites the first entry', 'HashMap throws an exception immediately'],
        correctIndex: 0,
        explainCorrect: "Right — collisions are handled by chaining/treeifying, not overwriting.",
        explainIncorrect: "Not quite — nothing is silently overwritten, and no exception is thrown for a normal collision.",
        hint: 'Think about two books landing on the same shelf peg — does the second book replace the first, or do they just hang together?',
      },
    ],
    interviewChallenge: [
      "Why does a poorly written hashCode() (e.g. always returning 0) silently destroy HashMap's performance?",
      "What changed about HashMap's internals in Java 8 regarding long collision chains?",
    ],
  },
  {
    id: 'real-interview-scenarios',
    order: 61,
    tier: 'interview-mastery',
    title: 'Real Interview Scenarios',
    tagline: 'Symptoms first, root cause second',
    expandReaction: "Last one — this is where all the previous concepts get pulled together into a live debugging conversation.",
    analogy: {
      emoji: '🧭',
      text: "A 'what would you do' interview scenario is like a doctor describing symptoms and asking for a diagnosis, not a memorized textbook definition. 'The API returns stale data sometimes, only under load' isn't asking you to define caching — it's asking you to reason through a real system, out loud, step by step.",
    },
    explainer: "Scenario-based interview questions test whether you can connect concepts you already know (caching, concurrency, garbage collection pauses, connection pools) to a described real-world symptom, and narrow down a root cause methodically. The strongest answers name the concept, explain why it would produce exactly that symptom, and describe how you'd actually verify it before concluding.",
    codeExample: {
      label: 'Example prompt and a strong walkthrough',
      code: `// Prompt: "Our API returns stale data intermittently, only under\n// heavy concurrent load. Where do you look first?"\n//\n// A strong walkthrough, out loud:\n// 1. "Stale" + "intermittent" + "under load" suggests a caching or\n//    visibility issue, not a one-time bug.\n// 2. Check: is there a cache with a TTL that's too long, or no\n//    invalidation on writes?\n// 3. Check: could this be a Java Memory Model visibility issue — a\n//    field read by one thread not seeing another thread's recent\n//    write, because it isn't volatile or properly synchronized?\n// 4. Verify with logs/metrics before concluding — don't just guess.`,
      output: [
        'A weak answer: "Maybe it\'s a caching bug?" (a guess, no reasoning)',
        'A strong answer: names 2 concrete hypotheses, explains why each matches the symptom, states how to verify',
        '✓ Interviewers are scoring the reasoning process, not just whether you land on the "right" guess',
      ],
    },
    kaiClaim: {
      text: "So the goal in a scenario question is to guess the exact right answer as fast as possible?",
      isCorrect: false,
      whyRight: "No — interviewers are mainly scoring your reasoning process: how you connect the symptom to plausible causes and how you'd verify them. A confident guess with no reasoning scores far worse than a methodical walkthrough.",
    },
    quiz: [
      {
        id: 'real-interview-scenarios-q1',
        question: "What are interviewers primarily evaluating in a 'here's a bug, what would you check' scenario question?",
        options: ["Your reasoning process — connecting symptoms to plausible causes and how you'd verify them", 'Whether you can guess the exact bug on the first try', 'How quickly you can recite a textbook definition'],
        correctIndex: 0,
        explainCorrect: "Right — the reasoning process is what's actually being scored.",
        explainIncorrect: "Not quite — landing on the exact bug immediately isn't required, and reciting a definition isn't the point of a scenario question.",
        hint: 'Think about the doctor-diagnosing-symptoms analogy again — is a confident guess with no explanation actually a good diagnosis?',
      },
    ],
    interviewChallenge: [
      'Next time you hit a real bug, could you narrate your reasoning out loud, the way you would in an interview?',
      "Why is 'let me check the logs/metrics first' usually a stronger answer than an immediate guess?",
    ],
  },
];

const PYTHON_CONCEPTS = [
  {
    id: 'variables-dynamic-typing',
    order: 1,
    title: 'Variables & Dynamic Typing',
    tagline: 'A sticky note, not a labeled box',
    expandReaction: "Good starting point — this is the first thing that surprises people coming from Java or C#.",
    analogy: {
      emoji: '📝',
      text: "A Python variable is like a sticky note with a name on it, stuck onto whatever value you want right now. You can peel it off an apple and stick it onto a car — same note (variable name), completely different thing underneath.",
    },
    explainer: "In Python, a variable doesn't have a fixed type — it's just a name pointing at a value. You can reassign that same name to a completely different type of value later, and Python won't complain, because the name was never locked to one type in the first place.",
    codeExample: {
      label: 'In real code',
      code: `x = 5\nprint(x)\n\nx = "hello"\nprint(x)\n\nx = [1, 2, 3]\nprint(x)`,
      output: [
        'x → 5',
        'x → "hello"',
        'x → [1, 2, 3]',
        '✓ Same variable name, three different types, no errors',
      ],
    },
    kaiClaim: {
      text: "So once I make x a number, it's stuck being a number forever, right?",
      isCorrect: false,
      whyRight: "Nope — in Python you can reassign a variable to any type, anytime. The name was never locked to one type.",
    },
    quiz: [
      {
        id: 'variables-dynamic-typing-q1',
        question: "Why doesn't Python complain when x changes from a number to a string?",
        options: ["Python variables aren't locked to one type — they just point to whatever value you assign", 'Python secretly converts the string to a number', 'x actually becomes two separate variables'],
        correctIndex: 0,
        explainCorrect: "Right — a Python variable is just a label, not a typed container.",
        explainIncorrect: "Not quite — Python doesn't convert anything or split the variable, it just lets the name point at a new value.",
        hint: "Think about the sticky note again — does the note care what it's stuck onto?",
      },
    ],
  },
  {
    id: 'lists-vs-tuples',
    order: 2,
    title: 'Lists vs Tuples',
    tagline: 'A whiteboard vs an engraved plaque',
    expandReaction: "Comes up constantly — this distinction trips up a lot of people in interviews.",
    analogy: {
      emoji: '🪧',
      text: "A list is like a whiteboard — you can erase and rewrite items anytime. A tuple is like an engraved plaque — once it's made, nobody can change what's carved into it.",
    },
    explainer: "Lists (written with square brackets []) are mutable — you can add, remove, or change items after creation. Tuples (written with round brackets ()) are immutable — once created, their contents can never change. Tuples are often used for fixed data that shouldn't accidentally be modified, like coordinates.",
    codeExample: {
      label: 'In real code',
      code: `my_list = [1, 2, 3]\nmy_list.append(4)\nprint(my_list)\n\nmy_tuple = (1, 2, 3)\n# my_tuple.append(4)  # this would crash`,
      output: [
        'my_list → [1, 2, 3, 4]',
        '✓ Lists can grow and change after creation',
        '✓ Tuples cannot be changed once created',
      ],
    },
    kaiClaim: {
      text: "So a tuple is basically just a list with round brackets instead of square ones — functionally identical?",
      isCorrect: false,
      whyRight: "Not quite — the brackets are a hint, but the real difference is tuples can't be modified after creation, and lists can.",
    },
    quiz: [
      {
        id: 'lists-vs-tuples-q1',
        question: 'Why would you choose a tuple over a list for storing a GPS coordinate (latitude, longitude)?',
        options: ["Because coordinates shouldn't accidentally change after being set", 'Because tuples are faster to type', 'Because Python requires coordinates to be tuples'],
        correctIndex: 0,
        explainCorrect: "Exactly — immutability protects fixed data like coordinates from accidental changes.",
        explainIncorrect: "Not quite — Python doesn't require this, and typing speed isn't the reason. It's about protecting data that shouldn't change.",
        hint: "Think about the engraved plaque again — why would you want something un-editable?",
      },
    ],
  },
  {
    id: 'dictionaries',
    order: 3,
    title: 'Dictionaries',
    tagline: 'Look up a word, get its meaning',
    expandReaction: "One of the most-used data structures in real Python code — worth knowing cold.",
    analogy: {
      emoji: '📖',
      text: "A Python dictionary works just like a real word dictionary — you look up a word (the key) and get its meaning (the value). You don't flip through page by page; you jump straight to the word you want.",
    },
    explainer: "A dictionary stores data as key-value pairs. Instead of accessing items by position (like a list), you access them by their key — direct lookup, not scanning from the start. Since Python 3.7, dictionaries also remember the order items were inserted in.",
    codeExample: {
      label: 'In real code',
      code: `student = {"name": "Amit", "age": 23}\nprint(student["name"])\n\nstudent["age"] = 24\nprint(student["age"])`,
      output: [
        'student["name"] → "Amit"',
        'student["age"] → 24',
        '✓ Looked up and updated directly by key, not position',
      ],
    },
    kaiClaim: {
      text: "So dictionaries keep things in the order I typed them, just like a list?",
      isCorrect: true,
      whyRight: "Actually yes, since Python 3.7 — dictionaries do remember insertion order. Good catch.",
    },
    quiz: [
      {
        id: 'dictionaries-q1',
        question: 'How do you retrieve a value from a dictionary?',
        options: ['By its key, not its position', 'By counting from the start like a list', "By its value"],
        correctIndex: 0,
        explainCorrect: "Right — dictionaries are looked up by key, direct access, no scanning needed.",
        explainIncorrect: "Not quite — dictionaries aren't accessed by counting position like a list, they're looked up directly by key.",
        hint: "Think about the real dictionary again — do you flip to page 47, or look up the word directly?",
      },
    ],
  },
  {
    id: 'list-comprehension',
    order: 4,
    title: 'List Comprehension',
    tagline: 'A factory line, not a loop you build by hand',
    expandReaction: "This is the concept that makes Python code suddenly look 'Pythonic' — worth mastering.",
    analogy: {
      emoji: '🏭',
      text: "Writing a for-loop to build a list is like assembling something by hand, one bolt at a time. A list comprehension is like a factory line — you describe what should happen to each item, and Python builds the whole list in one motion.",
    },
    explainer: "A list comprehension is a compact way to build a new list by applying an expression to every item in an existing sequence, all on one line. Under the hood it does exactly what an equivalent for-loop would do — it's just a shorter way to write it.",
    codeExample: {
      label: 'In real code',
      code: `nums = [1, 2, 3, 4]\nsquares = [n * n for n in nums]\nprint(squares)`,
      output: [
        'squares → [1, 4, 9, 16]',
        '✓ Same result as a for-loop, written in one line',
      ],
    },
    kaiClaim: {
      text: "So list comprehensions are a totally different language feature than for-loops, not related at all?",
      isCorrect: false,
      whyRight: "Not really — a list comprehension is just a shorter way to write a for-loop that builds a list. Under the hood it's doing the same thing.",
    },
    quiz: [
      {
        id: 'list-comprehension-q1',
        question: 'What does [n * n for n in nums] actually build?',
        options: ['A new list with each number squared', 'It modifies nums in place', 'It only keeps even numbers'],
        correctIndex: 0,
        explainCorrect: "Right — it builds a brand new list, leaving the original nums untouched.",
        explainIncorrect: "Not quite — nums itself isn't modified, and there's no filtering for even numbers here. A new squared list is created.",
        hint: "Look at what's on the left of the word 'for' — that's the expression applied to every item.",
      },
    ],
  },
  {
    id: 'functions-default-args',
    order: 5,
    title: 'Default Arguments',
    tagline: 'A pre-filled order form',
    expandReaction: "Small feature, but it comes up in almost every real codebase.",
    analogy: {
      emoji: '📋',
      text: "A function with a default argument is like an order form with a pre-filled default choice — you can accept the default, or cross it out and write your own answer instead.",
    },
    explainer: "A default argument gives a function parameter a fallback value that's used only when the caller doesn't provide their own. This lets you call the function with fewer arguments in the common case, while still allowing full customization when needed.",
    codeExample: {
      label: 'In real code',
      code: `def greet(name="friend"):\n    print(f"Hello, {name}!")\n\ngreet()\ngreet("Amit")`,
      output: [
        'greet() → "Hello, friend!"',
        'greet("Amit") → "Hello, Amit!"',
        '✓ Same function, default used only when no argument is given',
      ],
    },
    kaiClaim: {
      text: 'So once I set name="friend" as the default, I can never call greet with a different name?',
      isCorrect: false,
      whyRight: "You definitely can — the default is only used when you don't provide your own value. Pass any name you want and it overrides the default.",
    },
    quiz: [
      {
        id: 'functions-default-args-q1',
        question: 'What happens if you call greet() with no arguments?',
        options: ['It uses the default value "friend"', 'It crashes because name has no value', 'It waits for user input'],
        correctIndex: 0,
        explainCorrect: "Exactly — the default value fills in automatically when nothing is passed.",
        explainIncorrect: "Not quite — Python doesn't crash or wait for input here, it just falls back to the default value.",
        hint: "Think about the order form again — what happens if you leave that field blank?",
      },
    ],
  },
  {
    id: 'decorators',
    order: 6,
    title: 'Decorators',
    tagline: 'Gift wrap around a function',
    expandReaction: "This one takes people a couple of tries to click — worth reading twice.",
    analogy: {
      emoji: '🎁',
      text: "A decorator is like gift wrap around a present — the present (the function) inside is unchanged, but the wrapping adds something extra around it before anyone opens it.",
    },
    explainer: "A decorator is a function that wraps another function, adding extra behavior before or after it runs, without modifying the original function's own code. The @ syntax is just a convenient way to apply that wrapping.",
    codeExample: {
      label: 'In real code',
      code: `def shout(fn):\n    def wrapper():\n        return fn().upper()\n    return wrapper\n\n@shout\ndef greet():\n    return "hello"\n\nprint(greet())`,
      output: [
        'greet() → "HELLO"',
        '✓ Original greet() still just returns "hello" — @shout wraps extra behavior around it',
      ],
    },
    kaiClaim: {
      text: "So @shout rewrites the inside of greet() to add .upper() directly into its code?",
      isCorrect: false,
      whyRight: "No — greet() itself never changes. The decorator wraps a new layer around it instead of editing its insides.",
    },
    quiz: [
      {
        id: 'decorators-q1',
        question: 'What does @shout actually do to the greet function?',
        options: ["Wraps it so its output gets uppercased, without changing greet's own code", 'Deletes the original greet function', 'Renames greet to shout'],
        correctIndex: 0,
        explainCorrect: "Right — the original function is untouched, just wrapped with extra behavior.",
        explainIncorrect: "Not quite — greet isn't deleted or renamed, it's wrapped by another function that adds behavior around it.",
        hint: "Think about the gift wrap again — does wrapping a present change what's inside it?",
      },
    ],
  },
  {
    id: 'exception-handling',
    order: 7,
    title: 'Exception Handling',
    tagline: 'A safety net under a tightrope',
    expandReaction: "Last one — get this solid and your code stops crashing on the first unexpected input.",
    analogy: {
      emoji: '🪢',
      text: "Code that might fail is like walking a tightrope — a try/except block is the safety net underneath. If you fall (an error happens), the net catches you instead of the whole show stopping.",
    },
    explainer: "A try block runs code that might raise an error. If it does, the matching except block catches that specific error and runs recovery code instead of letting the whole program crash. Without a matching except, an error still crashes the program.",
    codeExample: {
      label: 'In real code',
      code: `try:\n    result = 10 / 0\nexcept ZeroDivisionError:\n    print("Can't divide by zero!")`,
      output: [
        'result = 10 / 0 → 💥 error happens here',
        'except ZeroDivisionError catches it',
        '"Can\'t divide by zero!"',
        '✓ Program keeps running instead of crashing',
      ],
    },
    kaiClaim: {
      text: "So if I don't write an except block at all, Python just silently ignores any error?",
      isCorrect: false,
      whyRight: "No — with no except block, an error crashes the program. try/except only helps if you actually catch it.",
    },
    quiz: [
      {
        id: 'exception-handling-q1',
        question: "What happens to the program if the code inside try fails and there's a matching except block?",
        options: ['The except block runs and the program keeps going', 'The whole program crashes anyway', 'Python ignores the error silently with no message'],
        correctIndex: 0,
        explainCorrect: "Exactly — the except block catches the error, and the program continues.",
        explainIncorrect: "Not quite — with a matching except block, the program does NOT crash, and it's not silently ignored either. The except code runs.",
        hint: "Think about the safety net again — does the tightrope walker's show stop the moment they slip?",
      },
    ],
  },
];

const REACT_CONCEPTS = [
  {
    id: 'components',
    order: 1,
    title: 'Components',
    tagline: 'LEGO bricks for a webpage',
    expandReaction: "Good starting point — everything else in React builds on this idea.",
    analogy: {
      emoji: '🧱',
      text: "A React component is like a LEGO brick — a small, reusable piece you can snap into different parts of a page, or reuse many times, instead of building the whole wall from scratch every time.",
    },
    explainer: "A component is a reusable, self-contained piece of UI, written as a function that returns what should appear on screen. Once built, the same component can be reused anywhere on the page, as many times as needed, each instance independent of the others.",
    codeExample: {
      label: 'In real code',
      code: `function Greeting() {\n  return <h1>Hello!</h1>;\n}\n\n<Greeting />\n<Greeting />`,
      output: [
        '<Greeting /> → renders "Hello!"',
        '<Greeting /> → renders "Hello!" again',
        '✓ Same component reused twice on the page',
      ],
    },
    kaiClaim: {
      text: "So a component can only ever be used once per page, right?",
      isCorrect: false,
      whyRight: "Not at all — the same component can be reused as many times as you want, like the same LEGO brick in different spots.",
    },
    quiz: [
      {
        id: 'components-q1',
        question: 'Why do developers break a page into components instead of writing one giant block of HTML?',
        options: ['Each component can be reused and tested on its own', 'Components make the page load slower', 'React requires exactly one component per page'],
        correctIndex: 0,
        explainCorrect: "Right — reusable, testable pieces instead of one giant tangled block.",
        explainIncorrect: "Not quite — components don't slow the page down, and React doesn't limit you to one per page.",
        hint: "Think about LEGO bricks again — why build with bricks instead of carving one solid block?",
      },
    ],
  },
  {
    id: 'props-vs-state',
    order: 2,
    title: 'Props vs State',
    tagline: 'A letter you\'re handed vs your own notebook',
    expandReaction: "Classic interview question — this trips up a lot of people who\'ve only used React casually.",
    analogy: {
      emoji: '✉️',
      text: "Props are like a letter someone hands you — you can read it, but you can't rewrite what it says. State is like your own notebook — you can update it yourself whenever you want.",
    },
    explainer: "Props are data passed into a component from its parent — read-only from the component's own perspective. State is data a component owns and manages itself, and can update over time, causing the component to re-render when it changes.",
    codeExample: {
      label: 'In real code',
      code: `function Welcome(props) {\n  return <p>Hi, {props.name}!</p>;\n}\n\n<Welcome name="Amit" />`,
      output: [
        '<Welcome name="Amit" /> → "Hi, Amit!"',
        '✓ Welcome cannot change props.name itself — only the parent that sent it can',
      ],
    },
    kaiClaim: {
      text: "So props and state are basically the same thing, just different names?",
      isCorrect: false,
      whyRight: "Not the same — props come from outside and can't be changed by the component itself; state is owned and changed by the component.",
    },
    quiz: [
      {
        id: 'props-vs-state-q1',
        question: 'Can a component change the value of a prop it receives?',
        options: ['No — props are read-only, only the parent passing them can change them', 'Yes, anytime it wants', 'Only if the prop is a number'],
        correctIndex: 0,
        explainCorrect: "Right — props flow one direction, from parent down, read-only for the child.",
        explainIncorrect: "Not quite — props aren't writable by the component receiving them, regardless of their type.",
        hint: "Think about the letter again — can the person who received it rewrite what it originally said?",
      },
    ],
  },
  {
    id: 'usestate-hook',
    order: 3,
    title: 'The useState Hook',
    tagline: 'A whiteboard the component owns',
    expandReaction: "This is the hook you'll use constantly — worth getting really comfortable with.",
    analogy: {
      emoji: '📋',
      text: "useState gives a component its own personal whiteboard. It can write something down (the initial value), and erase-and-rewrite it anytime (update the state) — and the moment it rewrites, the page redraws to match.",
    },
    explainer: "useState returns a piece of state and a function to update it. Calling the updater function doesn't just change a plain variable — it tells React to re-render the component with the new value, which is what actually makes the screen update.",
    codeExample: {
      label: 'In real code',
      code: `function Counter() {\n  const [count, setCount] = useState(0);\n  return <button onClick={() => setCount(count + 1)}>{count}</button>;\n}`,
      output: [
        'initial render → button shows 0',
        'click → setCount(1) called',
        'button re-renders → shows 1',
        '✓ Calling setCount tells React to redraw with the new value',
      ],
    },
    kaiClaim: {
      text: "So I could just write count = count + 1 directly instead of calling setCount, and the screen would still update?",
      isCorrect: false,
      whyRight: "No — React only knows to redraw the screen when you call the setter function (setCount), not from a plain variable reassignment.",
    },
    quiz: [
      {
        id: 'usestate-hook-q1',
        question: "What triggers the button's displayed number to update after a click?",
        options: ['Calling setCount, which tells React to re-render with the new state', 'The page automatically refreshes every second', 'Directly editing the count variable'],
        correctIndex: 0,
        explainCorrect: "Right — the setter function is what tells React something changed.",
        explainIncorrect: "Not quite — there's no auto-refresh timer, and directly editing a variable wouldn't trigger a re-render.",
        hint: "Think about the whiteboard again — does the room magically know you erased something, or do you have to announce it?",
      },
    ],
  },
  {
    id: 'useeffect-hook',
    order: 4,
    title: 'The useEffect Hook',
    tagline: '"Once the room is set up, do this"',
    expandReaction: "A lot of React bugs come from misunderstanding exactly when this runs — worth being precise about it.",
    analogy: {
      emoji: '🗒️',
      text: "useEffect is like a sticky note that says 'once the room is fully set up, do this task' — for example, fetching data right after the component first appears on screen, rather than trying to do it while the room is still being built.",
    },
    explainer: "useEffect runs code after the component renders, not during. Passing an empty array [] as the second argument means the effect runs exactly once, right after the first render — a common pattern for fetching initial data.",
    codeExample: {
      label: 'In real code',
      code: `useEffect(() => {\n  console.log("Component appeared!");\n}, []);`,
      output: [
        '(component renders)',
        'Component appeared!',
        '✓ The effect ran once, right after the first render, because [] means "no dependencies"',
      ],
    },
    kaiClaim: {
      text: "So useEffect with [] runs before the component even shows up on screen?",
      isCorrect: false,
      whyRight: "Actually the opposite — it runs right after the first render, once the component is already on screen, not before.",
    },
    quiz: [
      {
        id: 'useeffect-hook-q1',
        question: 'What does passing an empty array [] as the second argument to useEffect mean?',
        options: ['Run this effect once, right after the first render', 'Run this effect on every single render forever', 'Never run this effect at all'],
        correctIndex: 0,
        explainCorrect: "Right — an empty dependency array means 'just once, after the first render.'",
        explainIncorrect: "Not quite — [] doesn't mean 'run forever' or 'never run', it means the effect only fires once.",
        hint: "Think about the sticky note again — does it say 'do this once the room is ready' or 'keep doing this forever'?",
      },
    ],
  },
  {
    id: 'virtual-dom',
    order: 5,
    title: 'The Virtual DOM',
    tagline: 'A rough draft before the final copy',
    expandReaction: "This is the concept behind why React feels fast — worth understanding, not just memorizing.",
    analogy: {
      emoji: '📝',
      text: "Instead of rewriting the whole real page every time something changes (slow, like recopying an entire essay for one fixed typo), React first writes a quick rough draft (the virtual DOM), compares it to the last draft, and only updates the exact words that actually changed on the real page.",
    },
    explainer: "The virtual DOM is a lightweight in-memory copy of the page. When state changes, React builds a new virtual DOM, compares (diffs) it against the previous one, and only applies the minimal real changes needed to the actual page — instead of re-rendering everything from scratch.",
    codeExample: {
      label: 'In real code',
      code: `// Before: <p>Count: 3</p>\n// After:  <p>Count: 4</p>\n// React only updates the "3" -> "4" text,\n// not the whole <p> element or the rest of the page`,
      output: [
        'Compares old draft vs new draft',
        'Finds only the number changed',
        '✓ Updates just that one piece of the real page, not everything',
      ],
    },
    kaiClaim: {
      text: "So the virtual DOM completely replaces the real webpage — there's no real DOM anymore?",
      isCorrect: false,
      whyRight: "No — the virtual DOM is just a lightweight comparison draft. The real DOM (the actual page) still exists and gets selectively updated.",
    },
    quiz: [
      {
        id: 'virtual-dom-q1',
        question: 'Why does React use a virtual DOM instead of directly rewriting the whole page on every change?',
        options: ['To figure out the minimal real changes needed, instead of redrawing everything', "Because browsers don't allow direct DOM updates", 'Because it makes the code shorter to write'],
        correctIndex: 0,
        explainCorrect: "Right — the virtual DOM exists purely to compute the smallest necessary update.",
        explainIncorrect: "Not quite — browsers do allow direct DOM updates, and code length isn't the reason for the virtual DOM's existence.",
        hint: "Think about the rough draft again — why compare drafts instead of just recopying the whole essay every time?",
      },
    ],
  },
  {
    id: 'conditional-rendering',
    order: 6,
    title: 'Conditional Rendering',
    tagline: 'A bouncer at the door',
    expandReaction: "Simple idea, shows up in almost every real component you'll ever write.",
    analogy: {
      emoji: '🚪',
      text: "Conditional rendering is like a bouncer checking a condition before letting content onto the page — 'if logged in, show the dashboard; if not, show the login button' — only the right content gets through.",
    },
    explainer: "Conditional rendering means a component decides what to display based on a condition, usually with a ternary or if statement, rather than always showing the same fixed markup. The same component can return different content depending on its props or state.",
    codeExample: {
      label: 'In real code',
      code: `function Greeting({ loggedIn }) {\n  return loggedIn ? <p>Welcome back!</p> : <p>Please log in.</p>;\n}`,
      output: [
        '<Greeting loggedIn={true} /> → "Welcome back!"',
        '<Greeting loggedIn={false} /> → "Please log in."',
        '✓ Same component, different content based on the condition',
      ],
    },
    kaiClaim: {
      text: "So conditional rendering means writing two totally separate components, one for each case?",
      isCorrect: false,
      whyRight: "No — it's one component that decides which content to return, based on a condition, not two separate components.",
    },
    quiz: [
      {
        id: 'conditional-rendering-q1',
        question: 'What decides which message actually gets shown to the user?',
        options: ['The value of the loggedIn prop at render time', 'Whichever message was written first in the code', 'Both messages always show together'],
        correctIndex: 0,
        explainCorrect: "Right — the prop's value at render time decides which branch runs.",
        explainIncorrect: "Not quite — code order and showing both messages aren't how this works. The condition's value decides.",
        hint: "Think about the bouncer again — does the order the rules were written matter, or does the actual condition at the door?",
      },
    ],
  },
  {
    id: 'lists-and-keys',
    order: 7,
    title: 'Lists & Keys',
    tagline: 'Name tags at a reunion',
    expandReaction: "Last one — small detail, but skipping it causes real, hard-to-spot bugs.",
    analogy: {
      emoji: '🏷️',
      text: "When React renders a list of items, keys are like name tags at a family reunion — even if people move around or the order changes, the name tag tells React exactly who's who, so it doesn't get confused about who moved where.",
    },
    explainer: "When rendering a list with .map(), each item needs a unique key prop so React can track which item is which across re-renders — especially important if the list can be reordered, filtered, or have items added/removed.",
    codeExample: {
      label: 'In real code',
      code: `const items = ['Apple', 'Banana', 'Cherry'];\nitems.map((item) => <li key={item}>{item}</li>);`,
      output: [
        'renders 3 <li> elements',
        'each tagged with its own key ("Apple", "Banana", "Cherry")',
        '✓ If the list reorders later, React uses these keys to track which item is which',
      ],
    },
    kaiClaim: {
      text: "So I could just use the array index (0, 1, 2) as the key every time, and it would always work perfectly?",
      isCorrect: false,
      whyRight: "It works for simple static lists, but if items get reordered, added, or removed, using the index as a key can confuse React about which item is which — a stable unique ID is safer.",
    },
    quiz: [
      {
        id: 'lists-and-keys-q1',
        question: 'Why does React ask for a unique key when rendering a list of items?',
        options: ['So it can correctly track which item is which, especially if the list changes order', 'Keys make the list render faster no matter what', 'Keys are just for developer readability, React ignores them'],
        correctIndex: 0,
        explainCorrect: "Right — keys let React correctly track identity across re-renders.",
        explainIncorrect: "Not quite — keys aren't ignored by React, and they're not primarily a speed feature. They're about correctly tracking which item is which.",
        hint: "Think about the name tags again — what goes wrong at the reunion if two people swap tags?",
      },
    ],
  },
];

export const TECH_TRACKS = [
  { id: 'java', name: 'Java', icon: '☕', topic: 'java', concepts: JAVA_CONCEPTS },
  { id: 'python', name: 'Python', icon: '🐍', topic: 'python', concepts: PYTHON_CONCEPTS },
  { id: 'react', name: 'React', icon: '⚛️', topic: 'react', concepts: REACT_CONCEPTS },
];

export const getTrack = (trackId) => TECH_TRACKS.find(t => t.id === trackId);
export const getConceptById = (trackId, conceptId) => getTrack(trackId)?.concepts.find(c => c.id === conceptId);
export const getNextConcept = (trackId, currentId) => {
  const concepts = getTrack(trackId)?.concepts || [];
  const idx = concepts.findIndex(c => c.id === currentId);
  return idx >= 0 && idx < concepts.length - 1 ? concepts[idx + 1] : null;
};

// Learning-path tiers. Not every track has concepts tagged with a tier yet
// (only Java's 44 concepts are tagged so far) — getConceptsByTier returns
// null for an untiered track, and the page falls back to a flat list for it.
export const TIERS = [
  { id: 'beginner', label: 'Beginner', emoji: '🟢' },
  { id: 'intermediate', label: 'Intermediate', emoji: '🔵' },
  { id: 'advanced', label: 'Advanced', emoji: '🟣' },
  { id: 'interview-mastery', label: 'Interview Mastery', emoji: '🔴' },
];

export const getConceptsByTier = (trackId) => {
  const concepts = getTrack(trackId)?.concepts || [];
  if (!concepts.some(c => c.tier)) return null;
  return TIERS
    .map(tier => ({ ...tier, concepts: concepts.filter(c => c.tier === tier.id) }))
    .filter(group => group.concepts.length > 0);
};

// Keywords used to surface real interview questions related to each concept —
// bridges the simplified analogy back into the actual question bank.
const RELATED_KEYWORDS = {
  java: {
    'classes-vs-objects': ['constructor', 'instantiat'],
    encapsulation: ['encapsulat'],
    inheritance: ['inherit'],
    polymorphism: ['polymorph'],
    abstraction: ['abstract'],
    interfaces: ['interface'],
    constructors: ['constructor'],
    'method-overriding': ['overrid'],
    'composition-vs-inheritance': ['composition'],
    'dependency-injection': ['@autowired', 'constructor injection', 'field injection'],
    'arraylist-vs-linkedlist': ['arraylist', 'linkedlist'],
    hashmap: ['hashmap'],
    linkedhashmap: ['insertion order'],
    'fail-fast-vs-fail-safe': ['concurrentmodification'],
    concurrenthashmap: ['concurrenthashmap'],
    jvm: ['jvm'],
    'garbage-collection': ['garbage collect'],
    'heap-memory': ['heap'],
    metaspace: ['metaspace'],
    'memory-leak': ['memory leak'],
    outofmemoryerror: ['outofmemoryerror'],
    strings: ['immutab'],
    exceptions: ['checked exception'],
    generics: ['generic'],
    streams: ['stream pipeline', 'java streams'],
    lambdas: ['functional interface'],
    'file-handling': ['try-with-resources'],
    multithreading: ['synchroniz', 'race condition'],
    'concurrent-collections': ['least recently used'],
    'design-patterns': ['builder pattern', 'singleton'],
  },
  python: {
    'variables-dynamic-typing': ['dynamic typ'],
    'lists-vs-tuples': ['tuple'],
    dictionaries: ['dictionar', 'dict'],
    'list-comprehension': ['comprehension'],
    'functions-default-args': ['default argument', 'keyword argument'],
    decorators: ['decorator'],
    'exception-handling': ['exception', 'try/except', 'try-except'],
  },
  react: {
    components: ['component'],
    'props-vs-state': ['props', 'state'],
    'usestate-hook': ['usestate'],
    'useeffect-hook': ['useeffect'],
    'virtual-dom': ['reconcil'],
    'conditional-rendering': ['conditional render'],
    'lists-and-keys': ['key prop'],
  },
};

export function getRelatedQuestions(trackId, conceptId, allQuestions, limit = 3) {
  const track = getTrack(trackId);
  if (!track) return [];
  const keywords = RELATED_KEYWORDS[trackId]?.[conceptId] || [];
  if (!keywords.length) return [];
  return allQuestions
    .filter(q => q.topic === track.topic && keywords.some(kw => q.body.toLowerCase().includes(kw)))
    .sort((a, b) => (b.upvotes || 0) - (a.upvotes || 0))
    .slice(0, limit);
}
